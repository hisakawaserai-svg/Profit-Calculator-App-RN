import { SaleRecordDetailScreen } from '@/screens/SaleRecordDetailScreen';

// 出品中タブのレコード詳細（SPEC §3.2 SaleRecordDetailView）。id はレコードの UUID
export default function ListingsRecordScreen() {
  return <SaleRecordDetailScreen />;
}
