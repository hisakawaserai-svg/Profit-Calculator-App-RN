import { SaleRecordDetailScreen } from '@/screens/SaleRecordDetailScreen';

// 実績タブのレコード詳細（SPEC §3.2 SaleRecordDetailView）。id はレコードの UUID
export default function SoldRecordScreen() {
  return <SaleRecordDetailScreen />;
}
