# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Pinned dependencies

`package.json` pins some packages to an exact version on purpose. `npx expo install --fix`
and `npm update` will silently undo those pins and break the iOS build. Read
`docs/DEPENDENCIES.md` before running either, and re-check it afterwards.
