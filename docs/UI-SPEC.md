# UI 全面改訂 仕様 — UI-SPEC

出典: Claude Design プロジェクト「フリマアプリUI設計の見直し」の `UI設計案.dc.html`
（<https://claude.ai/design/p/00205ddb-1a3a-4279-abfd-3a7ab68d3597>）。
設計案は 0〜9 の 10 ターンからなり、**ターン 9 で採用案が確定**している。

本書はその確定案（`3a` `8a` `3c` `3d` `7b` `5b`）を実装用に文章化したもの。
デザイン上の ID（`3a` など）は設計案側の option ID をそのまま引用する。

**本書の全項目は 2026-08-10 に確定済み**（§5 の要確認 14 件、§6 の SPEC 差分 11 件）。
未確定の項目は残っていない。この確定に伴い `docs/SPEC.md` と `docs/SPEC-V2.md` を改訂済み。

**電卓（§7）は設計案の対象外だったが、同日に別途刷新を確定した。** 設計案側の「電卓は変更なし」は §7 が置き換える。

**状態の切り替え（§8）は設計案ターン 15 で後から見直し、案 `15c` を確定した。**
設計案の当初案（`3d` の売却トグル・`3c` の切替リンクの挙動）は §8 が置き換える。

## 0. 位置づけと優先順位

- 本書は **UI の構成**（画面・配置・遷移）を定める。
- **本書は SPEC.md / SPEC-V2.md より後発であり、UI に関する記述が衝突する場合は本書が優先する。**
  衝突していた 11 点は §6 に列挙し、決定を SPEC.md / SPEC-V2.md 側へ反映済み。
- 計算式・DB スキーマは現状維持（SPEC.md §2、`src/logic/profit.ts`、`src/db/**`）。設計案でも「変更なし」と明記されている。
  **電卓（`src/components/MiniCalculator.tsx`）だけは設計案の外で刷新を決めた。§7 を参照。**
- 表示語は引き続き `src/logic/labels.ts` に集約する（SPEC-V2 §5.3）。
  本改訂で追加・変更する語も同ファイルに置き、画面で文字列を組み立てない。

---

## 1. 採用案の画面構成

### 1.1 計算タブ（案 `3a`）— `app/(tabs)/index.tsx`

**ねらい**: 結果を常に画面上部に置き、逆算を「おまけ」から対等な 2 択に格上げする。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | タイトル「利益計算」。右に「？」（使いかた）**のみ**。歯車は置かない（§6-7） |
| 2 | 固定バー（スクロール時のみ） | スクロール量 40px 超で上端にスライドイン。左に結果ラベル、右に結果額（30px 太字）。2 段目に「売上 ¥… ／ 経費 ¥…」と「内訳 ▼」。内訳を開くと送料・梱包・その他・販売手数料の 3 行 |
| 3 | 結果カード | 先頭に 2 択セグメント「{純利益 or 利益}を出す」/「目標から逆算」（§6-4） |
| 3a | ├ 利益側 | 中央に結果ラベル（14〜15px）と結果額（46px 太字・正=緑/負=赤）。右上に「クリア」。下に「内訳 ▼」→ 売上総額 / 送料 / 梱包・その他 / 販売手数料（オレンジ） |
| 3b | └ 逆算側 | 「目標の純利益 / 目標利益」入力行（グレー地・右寄せ 22px。§6-5）。下に「この値段で出せばよい」＋必要販売価格（46px 太字・青）＋「送料・手数料 N% を差し引いた後の金額です」 |
| 4 | 種別セレクタ | 「不用品 / 仕入品」の 2 択セグメント。結果カードの**下**・入力カードの**直上**（§5-1） |
| 5 | 入力カード | 行型の数値欄（行高 60px）。販売価格 / 仕入価格（仕入品のみ）/ 送料 / 手数料 N%（− ＋ ステッパー）。各金額行の右端に電卓ボタン（§7） |
| 6 | 折りたたみ | 「梱包材・その他を入力 ▼」→ 開くと梱包材・その他の 2 行 |
| 7 | 下端固定ボタン | 「この内容で記録する」（青・高さ 54px）。半透明地＋上境界線でリストから浮かせる |

**挙動**

- 逆算モード中は販売価格欄をグレーアウト（背景 `rgba(120,120,128,.12)`・文字 60% 不透明）して無効化する。
- 固定バーは逆算モードでは見出しが「必要な販売価格」、売上側が「必要な売上」に変わり、色が青になる。
- 内訳の開閉状態は結果カードと固定バーで独立（設計案では別 state）。
- 「クリア」は入力が空でないときだけ有効。押した直後の「元に戻す」表示は**今回は実装しない**（§5-8）。
- リセット（クリア）は金額をクリアし、**種別も設定の既定値に戻す**（SPEC-V2 §1.3 のまま）。

### 1.2 記録タブ（案 `8a`）— `app/(tabs)/records/index.tsx`（新規）

**ねらい**: 「出品中」「実績」の 2 タブと月別詳細画面を 1 画面に統合し、上部の固定段を 3 段に収める
（ヘッダ + 月バー + 合計行）。一覧から詳細へ 1 タップ。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | タイトル「記録」。右に ⌕（検索）・⇅（並び替え）・？ の 3 ボタン |
| 2 | 月バー | `◀　2026年8月 ▾　▶`。中央タップで期間シート。初期表示は今月。**今月では ▶、データのある最古の月では ◀ を無効化。全期間の間は両方を無効化**（`rgba(60,60,67,.25)`。§5-14） |
| 3 | 合計行（固定） | 左に集計 2 値、右端に**状態チップ**と**種別フィルタチップ**の 2 つ |
| 4 | 件数 | リスト上に「N 件」（13px・グレー） |
| 5 | リスト | カード内に行を積む。行高は 2 段（商品名＋金額 / メタ行） |
| 6 | 空表示 | 見出し＋本文＋（絞り込み中のみ）「絞り込みを解除」リンク |
| 7 | 追加ボタン | 画面左下・タブバーの上（`left:20px; bottom:96px`）。青の丸角ボタン「＋ 記録」 |

**合計行の出し分け**

| 状態 | 左の値 A | 左の値 B |
|---|---|---|
| 売れた記録 | 「この月の収支」/「全期間の収支」（緑） | 「経費」（赤） |
| 出品中 | 「出品中」= N 点（オレンジ） | 「**出品価格の合計**」（青。§6-3） |

設計案 `8c` にある 3 値目「いちばん長い N 日」は `8a` にないため**実装しない**。

**チップ**

- **状態チップ**（青地 `rgba(0,122,255,.12)`・青文字）: 「売れた記録 ▾」⇄「出品中 ▾」をタップでトグル。
- **種別フィルタチップ**（グレー地・黒文字）: 「すべて → 不用品 → 仕入品 → すべて」を巡回。

**行の出し分け**

| 状態 | 主金額（右上） | メタ行（左） | メタ行（右） |
|---|---|---|---|
| 売れた記録 | 純利益額・**ラベルなし**（正=緑 / 負=赤。§6-2） | 「{種別}　M/D 販売」（§6-1） | 「経費 ¥…」 |
| 出品中 | **出品価格**（黒） | 「{種別}　M/D 出品」 | 「売れたら 約¥…・N 日経過」 |

- 見込み額には**常に「約」を付ける**。送料が未入力かどうかの判定は行わない（§5-3）。
- 「N 日経過」は出品日起算・当日 0 日（§5-2）。

**期間シート**（月バー中央タップ）

見出し「表示する期間」。**「期間を指定」は置かない**（§5-5）。選べるのは全期間か 1 か月のいずれか。
データタブの月バーからも同じシートを開く（`MonthNavBar` と対で共用する）。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | 見出し | 「表示する期間」 |
| 2 | クイック選択 | 「今月」「先月」「全期間」の 3 ボタンを横並びで**先頭に固定**。スクロールしても常に先頭に来る |
| 3 | 月グリッド | 年ごとのブロック。各ブロックは**年見出し**（例「2026年」）＋ **4 列の月グリッド**（1〜12 月を 4 列 × 3 行）。年は**降順**（今年 → 去年 → …） |
| 4 | 凡例 | 下部に「記録あり / 記録なし」の 2 項目 |

- 表示する年の範囲は、**今年から「記録の最も古い月を含む年」まで**。その年の 1〜12 月はすべて並べる
  （最古の記録より前の月・未来の月も枠としては出る）。
- 記録が 1 件もないときは今年のみを表示する（派生決定。グリッドが空にならないようにする）。

**月セルの出し分け**

| 月 | 表示 | 押せるか | 押したとき |
|---|---|---|---|
| 記録のある月 | 通常（黒文字） | ○ | その月を表示 |
| 記録のない過去の月・今月 | **薄い表示**（`rgba(60,60,67,.25)`） | ○ | その月を表示（一覧は空表示になる） |
| 未来の月 | **薄い表示（記録のない月と同じ濃さ）** | × | — |

- 記録のない月と未来の月は**見た目では区別しない**。違いは押せるかどうかだけ。
- 「記録がある」の判定は**種別フィルタ・状態チップ・検索を無視した全記録**で行う（派生決定）。
  絞り込みでグリッドの見た目が変わると、期間選びの手がかりとして不安定になるため。

**ハイライト**

- 選択中の期間をハイライトする（青地 `rgba(0,122,255,.12)`・青文字。チップと同じ）。
- **「今月」を選んでいるときは、上部の「今月」ボタンとグリッドの該当月の両方をハイライトする。**
  「先月」も同様に両方をハイライトする。
- グリッドから直接その月を選んだ場合も同じ状態になる（「今月」経由か否かで区別しない）。
- 「全期間」を選んでいるときは「全期間」ボタンのみがハイライトされ、グリッドにハイライトは出ない。

**挙動**

- 月・クイック選択のいずれも、**選んだ時点で即座に反映してシートを閉じる**。確定ボタンは置かない。
- 未来の月は押しても何も起きない（シートは閉じない）。

> 実装はステップ 3（計算タブ・§4）の後に行う。

**空表示の文言**

| 状況 | 見出し | 本文 | 追加リンク |
|---|---|---|---|
| 絞り込みなし | この期間の記録はありません | 左下の ＋ を押すと記録できます | なし |
| 種別で絞り込み中 | 条件に合う記録がありません | 「{種別}」で絞り込んでいます。ほかの月にはあるかもしれません | 絞り込みを解除 |

**並び替えシート（⇅）**: 先頭に「絞り込みをすべて解除」を置く（設計案 `2e`）。頻度が低いので画面には出さない。
種別フィルタは合計行のチップへ移したので、シートには**並び替えのみ**を置く
（SPEC-V2 §7-10 の「ソートメニューに統合」は本改訂で解消。IMPROVEMENTS.md「種別フィルタの発見しにくさ」も解消）。

**検索（⌕）**: タップでヘッダ行を検索フィールドに差し替える。常時表示の検索バーは置かない（段数を増やさないため。§5-10）。

### 1.3 記録フォーム（案 `3c`）— `src/screens/RecordFormSheet.tsx`

**ねらい**: 伝票（レシート）型。金額を 1 枚のカードに縦に積み、下の行ほど結果に近づける。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | シートハンドル | 幅 40px のグラバー |
| 2 | シートヘッダ | 「キャンセル」／「新しい記録」／「保存」（太字・青） |
| 3 | 伝票カード見出し行 | 左に状態ドット（緑=売れた / 出品中は別色）＋「売れた記録」、右に切替リンク「出品中にする」（設計案 `4b`） |
| 4 | 伝票カード本体 | 商品名（22px・下線は青 1.5px のインライン入力）→ その下に「商品名（必須）」のキャプション |
| 5 | ├ 種別セレクタ | 「不用品 / 仕入品」。**商品名の直下・金額の積み上げの直前**（§6-6） |
| 6 | ├ 販売価格 | 右寄せ 24px 太字。下に区切り線 |
| 7 | ├ − 仕入価格 | **仕入品のときのみ表示**（§5-11）。赤 20px |
| 8 | ├ − 送料 | 赤 20px |
| 9 | ├ − 手数料 N% | オレンジ 20px |
| 10 | ├ ＋ 梱包材・その他 | 青リンク。未入力時は右に「未入力」（40% グレー） |
| 11 | └ 結果行 | 太い区切り線（1.5px）の下に「純利益 / 利益」＋結果額（30px 太字・緑） |
| 12 | 日付カード | 販売日（「今日（2026/08/09）」青）/ 出品日。各行に「今日・昨日・一昨日」のチップを常設し、それ以外は「日付 ▸」でカレンダーを開く（§8.10） |
| 13 | メモ | 「メモを書く ▼」の 1 行カード（折りたたみ） |

**挙動**

- 状態は見出し行のリンクで切り替える。上端に 2 択ボタンは置かない（金額の流れの前に無関係な操作が挟まるため。設計案ターン 4 の結論）。
  切り替えると **12 の日付カードの中で売れた日の行がその場で開く／閉じる**（§8.7）。フォームでは「保存」を押すまで書き込まない（§8.6）。
- 仕入品 → 不用品の切替では、**仕入価格をその場で 0 にクリアしてから行を消す**（SPEC-V2 §1.5）。
  種別セレクタと仕入価格行が同じカード内にあるので、変化がその場で見える。
- 販売日は出品中では非表示（SPEC.md §3.2 のまま）。
- 必須は商品名のみ（SPEC.md §5.2 のまま）。

### 1.4 レコード詳細（案 `3d`）— `app/(tabs)/records/[id].tsx` → `src/screens/SaleRecordDetailScreen.tsx`

**ねらい**: 金額の流れを 1 枚のレシートで見せ、編集・削除を下端の操作列にまとめる。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | 「◀ 記録」／右に「？」 |
| 2 | メタ行 | 状態バッジ（緑地・白文字「売れた」）＋「{種別} ・ 8/2 出品 → 8/9 販売（7日）」 |
| 3 | 商品名 | 26px 太字 |
| 4 | レシートカード | 販売価格（24px 太字）→ 区切り線 → −仕入価格（仕入品のみ）／− 送料（赤）／− 販売手数料 (N%)（オレンジ）／− 梱包材・その他（未入力なら 40% グレー）→ 太い区切り線 → 「純利益 / 利益」＋額（34px 太字） |
| 5 | 状態カード | 左に状態バッジ、右にボタン（出品中は「✓ 売れた」＝緑の塗り、売れた記録は「出品中に戻す」＝枠線）。売れた記録はその下に「売れた日」の行（**§8**。設計案のトグルは廃止。レイアウトは §8.9）。メタ行のバッジとは役割が違うので両方置く（§5-13） |
| 6 | メモ | 見出し「メモ」＋カード（未入力は「なし」） |
| 7 | 下端操作列 | 「編集する」（青・flex:1）／「削除」（白地・赤文字・幅 104px） |

**挙動**

- 削除は確認アラートを挟む（SPEC.md §5.4 のまま）。
- 種別の変更 UI は置かない。編集フォーム経由のみ（SPEC-V2 §1.3）。
- **画面下部の 1 件サマリー（`CareerSummarySection`）は置かない**（§5-12）。レシートカードの結果行が同じ役割を果たす。
- 経過日数は出品日起算・当日 0 日（§5-2）。売却済みは `saleDate − saleStartDate`、出品中は `今日 − saleStartDate`。

### 1.5 データタブ（案 `7b`）— `app/(tabs)/data/index.tsx`

**ねらい**: 切替を減らし、記録タブと同じ月バー＋固定合計行に揃える。グラフは収支のみ。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | タイトル「データ」。右に「？」 |
| 2 | 月バー | 記録タブと同一（`◀ 2026年8月 ▾ ▶`。無効化の規則も同じ） |
| 3 | 合計行（固定） | 「売上」（青）/「収支」（緑）/「経費」（赤）の 3 値＋右端に種別フィルタチップ |
| 4 | グラフカード | 見出し「収支の推移」＋**凡例**（下記）。**棒と折れ線の組み合わせ**。選択中の棒のみ濃色（`#34C759`）、他は 30% 不透明 |
| 5 | 選択日の一覧 | 「8月9日の記録　N件」＋右に「選択を解除」。下に記録行のカード（記録タブと同じ `RecordRow`。§6-11） |
| 6 | 注記 | 「全期間を選ぶと刻みが『月ごと』に変わり、見出しも『全期間の収支』になります。」 |

**廃止する切替（設計案ターン 6 の決定）**

| 廃止するもの | 代替 |
|---|---|
| 「今年」チップ | 全期間で代用 |
| 指標切替（売上金額 / 収支） | 合計行に売上・収支・経費の 3 値を常時表示。売上の**推移**は見られなくなる（§5-4 で許容） |
| 表示単位切替（明細 / 日別 / 月別 / 年別） | **月を選択 = 日ごと / 全期間 = 月ごと**の 2 分岐。設計案 `6b` の 62 日規則は採らない（§5-5） |
| 「明細」（1 件ずつの折れ線） | 棒タップで下に出る一覧が同じ役割を果たす |
| 「年別」 | 上記の 2 分岐に含めて廃止 |
| 期間指定（`startDate` / `endDate` の自由指定と ◀▶ の平行移動） | 月バー＋期間シート（全期間 / 各月）に置き換え（§5-5） |

**挙動**: グラフをタップするとその日の記録が下に並ぶ。行タップでレコード詳細へ。
判定は**スロット 1 つぶんの縦の列**が受け、**最も近い「記録のある」棒**を選ぶ（下記）。

#### グラフの中身（後日の変更）

設計案 `7b` は「収支の棒グラフ 1 本」だったが、2 回の変更を経て**棒と折れ線の組み合わせ**に落ち着いた。
1 回目は実利用者の好みで折れ線に差し替え、2 回目に**累計を読みたい**という要求が足され、
「刻みごとの収支」と「期間の初めからの累計収支」という**別々の量**を 1 枚に重ねる形になった。

| 要素 | 表すもの | 色 | 軸 |
|---|---|---|---|
| 棒 | その日／その月の収支 | 緑（`#34C759`） | **左** |
| 折れ線 | 期間の初めからの累計収支 | 藍（`indigo`） | **右** |

- **累計の起点は表示中の期間の先頭**で、アプリ全体の通算ではない。期間を選び直せば起点も動く。
  最後の値は合計行の収支と一致する（同じ集合の合計なので、見比べても食い違わない）。
- 累計は棒より 1 桁以上大きくなるので**軸を分ける**。ただし **0 の高さは両軸で揃える** ──
  桁を合わせるために 0 の位置までずらすと、「棒は黒字なのに線は赤字」に見える読み違いが起きる。
  実装は上限を軸ごとに決めたあと、負側が占める割合を大きい方に合わせる（`logic/analytics.ts` の `dualAxisBounds`）。
- **凡例**（見出しの直下）に「{日ごと / 月ごと}の収支」（棒の見本）と「累計収支」（線の見本）を出す。
  棒の側の語が**刻みの表示を兼ねる** ── 旧・見出し右の刻み表示はこの語に畳んだ。
  棒が何かを言えば刻みも言えるので、「日ごと」を 2 か所に出してグラフ 1 つに説明を 2 段付けない。
- **記録のある集計点が 1 つしかない期間では、折れ線・右軸・凡例の 2 行目を出さない**（派生決定）。
  1 点の累計はその点自身で、左右の軸に同じ数字が並び、棒の上に点が 1 つ乗るだけになる ──
  説明するものがないのに説明の道具だけが増える。記録を始めた直後や、全期間に月が 1 つしかないときは
  常にこの状態になるので、例外扱いにはできない。

「明細」（1 件ずつの折れ線）の廃止（上の表）とは別の話 ── 廃止したのは**刻み**としての明細で、
ここでの折れ線が担うのは累計。

#### X 軸は日付の軸

**記録のある集計点だけを詰めて並べない。** 期間の全スロット（日ごと / 月ごと）を等間隔に置き、
記録のない位置は**棒なし**（累計は横ばい）で空ける。詰めて並べると、7/1 と 7/31 にだけ記録がある月で
2 本の棒が隣り合ってしまい、30 日空いていることが読めない。空けることで
「月末に集中して売れた」「月初に売れて後は動きがない」が形として出る。

| 期間 | 軸が覆う範囲 |
|---|---|
| 過去の月 | その月の 1 日 〜 末日 |
| 今月 | 1 日 〜 **今日**（まだ来ていない日まで伸ばすと右半分が常に空になる） |
| 全期間 | 最も古い記録の月 〜 今月 |

- 組み立ては `logic/analytics.ts` の `chartSpan` / `chartSlots` / `densifySeries`（純粋関数＋単体テスト）。
- **棒・累計線・タップ列・X 軸ラベルは同じ座標系**（スロット i の中心 = `initialSpacing + i × pitch + 棒幅/2`）。
  ラベルだけはライブラリが「枠の中央」に置くため、枠を広げると**棒の右へずれる**
  （実機で確認: 7/1 の棒が 07/01 ラベルより左、7/31 の棒が 07/29 ラベル付近に見えた）。
  枠を広げたぶんを打ち消して棒の中央へ戻す。
- **ラベルは両端に必ず打つ**（`labelSlotIndices`）。先頭から一定間隔で打つだけだと、
  間隔が期間の長さを割り切るときしか末尾に当たらない ── 31 日の月は 1・8・15・22・29 日で止まり、
  **月末がラベルに現れない**（30 日の月では 5 日ぶんが無記名で残り、最後の棒が軸の手前で浮いて見える）。
  間隔は保ったまま末尾を必ず打ち、末尾が直前と近すぎる（間隔の半分以下）ときは直前を落とす。
  これで月の日数（28 / 29 / 30 / 31 日）に関わらず、軸の右端は必ずその月の最終日になる。
  ラベルとスロットの左右に残る余白は、両端に置いた同じ幅の内側余白（`initialSpacing` / `endSpacing`）。
- **横スクロールはさせない。** 期間ぶんを画面幅に収める ── 月内の分布を一目で読むのが主題で、
  スクロールするとその一目が失われる。そのぶん 1 スロットは細くなる（31 日の月で約 7pt）。

**タップ判定は「最も近い棒」**（派生決定）。日付の軸にするとスロットの大半が空になり得るので、
押した位置ちょうどに記録がないことが普通に起きる。またライブラリの判定は**棒の矩形そのもの**で、
細い棒・低い棒・空きスロットは押せない。そこでグラフの上に**スロット 1 つぶんの透明な列**（高さいっぱい）を
重ね、押された位置から外側へ広げながら記録のある棒を探す（`nearestRecordedIndex`）。
縦は狙わなくてよくなり、空振りに見える操作もなくなる。同じ距離に 2 つあるときは古い側を採る。

#### 目盛りはキリのいい数

素の上限（`max(1000, 最大値) × 1.15`）をそのまま段数で割ると「8596 / 6447 / 4298 / 2149」のような
半端な数字が並ぶ。**1 段の幅を 1 / 1.5 / 2 / 3 / 5 × 10^n に丸め**、上限を「丸めた幅 × 段数」に取り直す
（`dualAxisBounds`）。左右それぞれの軸に適用し、**段数だけは両軸で共有**する ──
段数が違うと左右のラベルが別の高さに並び、罫線と対応しなくなる。段数を共有すれば
負側が占める割合（下の段数 ÷ 上の段数）も自動的に一致し、0 の高さが揃う。

例: 収支 7,475 円 → 左軸 3000 / 6000 / 9000、累計 12,685 円 → 右軸 5000 / 10000 / 15000。

#### 折れ線は自前で描く（実装メモ）

ライブラリ（`react-native-gifted-charts`）にも棒グラフへ線を重ねる機能（`showLine` / `lineConfig`）は
あるが、線を描く SVG の器の幅に **Y 軸ラベル幅が入っていない**ため、右端が必ず切れる
（実機で確認: 31 日の月では 26 日目あたりから線が消えた）。自前で `react-native-svg` の
`Polyline` を重ね、x はタップ列と同じ式で出す ── 同じ式を使うので棒との位置が必ず揃う。
右軸の**目盛り**は引き続きライブラリ（`secondaryYAxis`）が描く。

### 1.6 設定タブ（案 `5b`）— `app/(tabs)/settings/index.tsx`

**ねらい**: モーダルからタブへ昇格させ、毎日の入力の既定値を先頭に置く。使いかたは 1 行目で探させない。

**構成（上から）**

| # | 群 | 項目 |
|---|---|---|
| 1 | （見出しなし） | 「使いかた ▸」1 行カード。下に注記「各画面の右上の『？』からも、その画面の説明だけを開けます。」 |
| 2 | 記録の既定値 | 種別（不用品 / 仕入品 セグメント）／手数料（`10%` ＋ − ＋ ステッパー） |
| — | 注記 | 「新しく記録を作るときの初期値です。保存済みの記録は変わりません。」 |
| 3 | 入力を減らす（今後） | 梱包材の登録（準備中・非活性）／手数料のプリセット（準備中・非活性） |
| 4 | データ | 書き出し（CSV）（**準備中・非活性**。§5-7）／ 記録の件数（右に「N 件」） |
| 5 | フッタ | 「バージョン 2.0」（中央・26px 上マージン） |

- 設計案 `5b` の「送料（前回の値を使う）」は**今回は実装しない**（§5-6）。項目ごと置かない。
- 追加する設定は `defaultCommission`（手数料の既定値）のみ。`src/settings/index.ts` の `Settings` 型に足す。
- 「記録の件数」は `repository` に件数取得を 1 本足して表示する。

---

## 2. 画面遷移図

```
TabView（4 タブ）
├─ 計算: app/(tabs)/index.tsx（案 3a）
│    ├─(この内容で記録する)──> [sheet] RecordFormSheet（種別を引き継ぐ・SPEC-V2 §1.4）
│    └─(？)──────────────> [sheet] 使いかた（計算タブの節を開いた状態）
│
├─ 記録: app/(tabs)/records/index.tsx（案 8a）
│    ├─(行タップ)──────────> [push] app/(tabs)/records/[id].tsx（案 3d）
│    │      ├─(編集する)────> [sheet] RecordFormSheet（案 3c）
│    │      ├─(削除)────────> [alert] 削除確認
│    │      └─(？)──────────> [sheet] 使いかた（記録の節）
│    ├─(＋ 記録)───────────> [sheet] RecordFormSheet（案 3c）
│    ├─(⌕)─────────────────> ヘッダ行を検索フィールドに差し替え（画面内）
│    ├─(⇅)─────────────────> [sheet] 並び替え（先頭に「絞り込みをすべて解除」）
│    └─(月バー中央)─────────> [sheet] 表示する期間（今月 / 先月 / 全期間 ＋ 年ごとの月グリッド）
│
├─ データ: app/(tabs)/data/index.tsx（案 7b）
│    ├─(棒タップ)──────────> 当日の一覧（画面内・グラフ下に展開）
│    ├─(行タップ)──────────> [push] app/(tabs)/data/record/[id].tsx（画面は記録タブと同じ 3d）
│    ├─(月バー中央)─────────> [sheet] 表示する期間
│    └─(？)─────────────────> [sheet] 使いかた（データの節）
│
└─ 設定: app/(tabs)/settings/index.tsx（案 5b）
     └─(使いかた)───────────> [push] app/(tabs)/settings/help.tsx → HelpScreen

初回起動: 全画面シート 3 枚（計算する → 記録する → 見返す）。
          最後に「あとで見るには：設定 > 使いかた」と場所を明示。以降は自動表示しない。
          （実装優先度は最後・設計案 2e）
```

- **シート = 下から出る一時的な面。月別詳細画面（`SaleRecordScreen`）は廃止し、一覧から詳細へ 1 タップ。**
- 各画面の「？」は `HelpScreen` を**シート表示**し、開く節を props で渡す。設定タブの「使いかた」だけが push
  （タブをまたぐ push を書かずに済ませるため。§5-9）。

---

## 3. ファイル対応表

### 3.1 新規

| ファイル | 内容 |
|---|---|
| `app/(tabs)/records/_layout.tsx` | 記録タブのスタック |
| `app/(tabs)/records/index.tsx` | 記録タブ本体（案 `8a`） |
| `app/(tabs)/records/[id].tsx` | レコード詳細のルート（記録タブの入口） |
| `app/(tabs)/data/_layout.tsx` | データタブのスタック（詳細から**開いたタブへ戻る**ために持つ） |
| `app/(tabs)/data/record/[id].tsx` | レコード詳細のルート（データタブの入口）。画面の実体は記録タブと同じ `SaleRecordDetailScreen` |
| `app/(tabs)/settings/index.tsx` | 設定タブ（案 `5b`。モーダルから昇格） |
| `app/(tabs)/settings/help.tsx` | 使いかた（ヘルプタブから移動） |
| `src/screens/RecordListScreen.tsx` | `MonthlyRecordListScreen` と `SaleRecordScreen` を統合した 1 画面 |
| `src/components/MonthNavBar.tsx` | `◀ YYYY年M月 ▾ ▶`。記録タブ・データタブで共用 |
| `src/components/PeriodSheet.tsx` | 期間シート（クイック選択 ＋ 年ごとの月グリッド ＋ 凡例）。`MonthNavBar` と対で記録タブ・データタブが共用する（§1.2） |
| `src/logic/periodGrid.ts` | 月グリッドの組み立て（出す年の範囲・各マスの記録の有無と未来判定）。純粋関数＋単体テスト |
| `src/components/SummaryBar.tsx` | 固定の合計行（2〜3 値＋右端チップ） |
| `src/components/FilterChip.tsx` | 状態チップ・種別チップ |
| `src/components/CollapsibleSection.tsx` | 折りたたみ（内訳・梱包材・メモ） |
| `src/components/EmptyState.tsx` | 空表示（見出し＋本文＋任意の解除リンク） |
| `src/logic/listingDays.ts`（仮） | 経過日数の算出（§5-2）。純粋関数＋単体テスト |
| `src/logic/calcMemo.ts`（仮） | 電卓の行モデルと合計の算出（§7.6）。純粋関数＋単体テスト |
| `src/components/UndoBar.tsx`（仮） | 画面下部のバー（本文＋「元に戻す」・数秒で消える）。§8.3 |
| `src/components/CalendarPicker.tsx` | カレンダー形式の日付ピッカー（チップ＋盤面＋年月グリッド）。日付欄で共用（§8.10） |
| `src/logic/calendar.ts` | 盤面の組み立て・範囲判定・月送りの可否（§8.10）。純粋関数＋単体テスト |
| `src/logic/saleDate.ts` | 売れた日の選べる範囲と範囲内への寄せ（§8.5）。純粋関数＋単体テスト |
| `src/components/SheetModal.tsx` | 下から出るシートの共通の器（§3.5）。幕とシートを別々に動かす。期間シート・カレンダー・並び替え・電卓が共用 |

### 3.2 改修

| ファイル | 内容 |
|---|---|
| `app/(tabs)/_layout.tsx` | 5 タブ → 4 タブ（計算・記録・データ・設定） |
| `app/(tabs)/records/_layout.tsx` | `unstable_settings.anchor = 'index'`。[id] ルートへ外から直接入ったとき（ディープリンク・リロード）に一覧を下に積み、戻る導線を残す |
| `app/(tabs)/index.tsx` | 結果を上部へ・固定バー・逆算を対等な 2 択に・折りたたみ・下端に記録ボタン・歯車の削除 |
| `src/screens/SaleRecordDetailScreen.tsx` | カードをレシート型に（案 `3d`）。下部 1 件サマリーを削除。状態切替の配線（§8） |
| `src/components/RecordDetailSections.tsx` | レシート型に伴う再構成。売却トグル → 状態別ボタン＋「売れた日」の行（§8） |
| `src/screens/RecordFormSheet.tsx` | 伝票型・状態は見出し行・日付とメモは折りたたみ（案 `3c`）。切替で売れた日の行を開閉（§8.7） |
| `src/components/DateField.tsx` | 選択できる範囲（`minDate` / `maxDate`）を受け取る（§8.5）。ホイールを廃し、行内チップ＋カレンダーに（§8.10） |
| `src/screens/DataScreen.tsx` | 指標・単位・期間指定の切替を廃止、月バー＋合計行に（案 `7b`） |
| `src/logic/analytics.ts` | `ChartUnit` を `day` / `month` の 2 値に。`MetricType` / `CHART_UNITS` / `RESET_SPAN` / 期間の平行移動を削除。累計（`cumulativeProfits`）・2 軸の範囲（`dualAxisBounds`。キリのいい目盛りと 0 の高さ揃え）・日付の軸（`chartSpan` / `chartSlots` / `densifySeries`）・X 軸ラベルの位置（`labelSlotIndices`。両端を必ず打つ）・タップ判定（`nearestRecordedIndex`）を追加 |
| `src/logic/labels.ts` | §6-3 / §6-4 の語を追加、`metricLabel` を削除（§6-10） |
| `src/settings/index.ts` | `defaultCommission` を追加 |
| `src/db/repository.ts` | 件数取得を追加。`AnalyticsFilter` の期間を月キー／全期間に変更。`monthsWithRecords`（**絞り込みを取らない**月グリッド用の集合。§1.2）を追加 |
| `src/components/RecordRow.tsx` | 金額ラベルを削除し種別を表示（§6-1 / §6-2）。出品中は見込み額と経過日数を追加 |
| `src/components/OptionSheet.tsx` | 先頭に「絞り込みをすべて解除」。種別グループは合計行のチップへ移すので削除 |
| `src/components/NumericField.tsx` | 行型（ラベル左・数値右・行高 60px）の数値欄に。電卓へ行き先のラベルを渡す（§7.6） |
| `src/components/MiniCalculator.tsx` | シート化・計算メモ型の積み上げ・キーパッド刷新（§7） |
| `src/logic/calculator.ts` | `×` `÷` を受け付ける（評価直前に `*` `/` へ置換）。§7.6 |
| `src/screens/HelpScreen.tsx` | 開く節を props で受け取り、その節を開いた状態で表示 |

### 3.3 廃止

| ファイル | 理由 |
|---|---|
| `app/(tabs)/listings/**` | 出品中タブを記録タブへ統合 |
| `app/(tabs)/sold/**` | 実績タブを記録タブへ統合 |
| `app/(tabs)/help.tsx` | ヘルプタブ廃止 → 設定タブ配下へ |
| `app/settings.tsx` | 設定モーダル廃止 → 設定タブへ昇格 |
| `src/screens/SaleRecordScreen.tsx` | 月別詳細画面の廃止 |
| `src/screens/MonthlyRecordListScreen.tsx` | `RecordListScreen` へ統合 |
| `src/components/MonthlySummaryCard.tsx` | 月セクションの廃止（合計は固定の合計行へ） |
| `src/components/SummarySection.tsx` | 画面下部の累計セクションの廃止 |
| `src/components/PlaceholderScreen.tsx` | 参照元がなくなる |
| `src/components/WheelColumn.tsx` | ホイール型ピッカーの全廃（§8.10.5）。`DateField` の差し替えで参照元がなくなる |
| `src/components/MonthPickerSheet.tsx` | 期間シート（§1.2 の月グリッド）に置き換わり、参照元がなくなる |

### 3.4 変更なし

`src/logic/profit.ts` / `src/logic/kindFilter.ts` / `src/db/schema.ts` /
`src/logic/input.ts` — 計算式・種別の絞り込み・DB スキーマ・数値入力のフィルタは現状維持。

`src/logic/input.ts` を変えないことは電卓の刷新でも維持する（§7.4。合計が負のときは「入れる」を無効にして対処する）。
**`src/components/MiniCalculator.tsx` は §3.2 の改修に移った**（設計案では「変更なし」だったが §7 で刷新を決定）。

### 3.5 下から出るシートの共通の器（実装後の追補）

`Modal animationType="slide"` は**中身を丸ごと**下から動かすため、暗い幕まで一緒にせり上がってきて、
地の画面に幕が降りたようには見えなかった。`src/components/SheetModal.tsx` で 2 つを別々に動かす:

| 要素 | 動き |
|---|---|
| 幕（`rgba(0,0,0,.3)`） | **画面全体に固定**したまま、不透明度だけ 0 → 1 |
| シート | 画面の下端の外から定位置へ（`translateY`） |

- 閉じるときは逆再生する。**下がり切ってから `onClose` を呼ぶ**のがこの器の役目で、中身から閉じる操作
  （「閉じる」・選んで即閉じる行・「入れる」）はすべて器が渡す `close` を通す。直接 `onClose` を呼ぶと
  親がその場でアンマウントしてしまい、下がるところが見えない。
- 対象は下から出る 4 つのシート（期間シート・カレンダー・並び替え / オプション・電卓）。
  記録フォーム（`RecordFormSheet`）は iOS 純正の `presentationStyle="pageSheet"` で、幕も含めて
  OS が出すので**この器は使わない**。
- 時間は開く 260ms（減速）／閉じる 200ms（加速）。

---

## 4. 実装順

| # | 内容 | 採用案 | 完了条件 |
|---|---|---|---|
| 1 | タブ 4 本化と設定タブの昇格 | `5b` | 4 タブで起動し、設定タブから既定種別を変えられる |
| 2 | 記録タブの統合と月別詳細の廃止 | `8a` | 出品中/実績/月別詳細の 3 画面が 1 画面になり、一覧から詳細へ 1 タップで行ける |
| 3 | 計算タブの結果上部化と逆算 | `3a` | 結果が常に上部にあり、逆算が対等な 2 択になっている |
| 4 | 記録フォームと詳細 | `3c` `3d` | 保存・編集・削除が伝票型 UI で一巡する |
| 5 | データタブの簡素化 | `7b` | 指標・単位・期間指定の切替がなく、刻みが期間から自動で決まる |
| 6 | 各画面の「？」と空表示 | `2e` | 「？」から該当セクションが開き、0 件時に空表示が出る |
| 7 | 初回起動ガイド | `2e` | シート 3 枚。**優先度は最後** |
| 8 | 電卓の刷新 | —（§7） | 下から出るシートで行を積み上げ、合計だけが呼び出し元の欄に入る |

- ステップ 1 と 2 の間で止めても壊れない（タブが減り設定が移るだけ）。
- ステップ 3・4・5 は互いに独立なので順序を入れ替えてよい。
- **ステップ 8 は他のどのステップとも独立**（電卓は `NumericField` の内側で完結し、画面構成に触れない）。
  番号は最後だが、ステップ 3・4 の後ならいつ入れてもよい。
- **ラベルの変更（§6-1〜§6-4）は `labels.ts` を先に直す。** ステップ 2 と 3 の両方が依存する。

---

## 5. 要確認への決定

設計案が決めていない・現状のコードに存在しない事項。**14 件すべて確定済み。**

### 5-1. 計算タブの種別セレクタの位置

設計案 `3a` は結果カードの下・入力カードの上に置いている。SPEC-V2 §1.3 の記述は「画面上部」。

**決定: 設計案どおり、結果カードの下・入力カードの直上に置く。**
仕入価格欄の直上なので、SPEC-V2 §1.5 の「切替で仕入価格欄が消えるのを目視できる」要件を満たす。
SPEC-V2 §1.3 の「画面上部」を「入力カードの直上」に改訂済み。

### 5-2. 「N 日経過」の数え方

**決定: 起点は `saleStartDate`（出品日）。日数 = 基準日 − 出品日 の暦日差で、出品日当日は 0 日。**

| 状態 | 基準日 |
|---|---|
| 売却済み | `saleDate` |
| 出品中 | 今日 |

設計案 `3d` の「8/2 出品 → 8/9 販売（7日）」と一致する。
`src/logic/` に純粋関数として置き、単体テストを付ける（時刻ではなく暦日で数えること）。

### 5-3. 送料未入力の判定

`postage` は NOT NULL DEFAULT 0 のため、「未入力」と「送料 0 円（送料込み・手渡し）」を区別できない。
設計案は `postage === 0` で「約」と「（送料未定）」を出し分けている。

**決定: スキーマは変更しない。出品中の見込み額には常に「約」を付け、送料未入力かどうかの判定は行わない。**
「（送料未定）」は出さない。行のメタ行は常に「売れたら 約¥…・N 日経過」。
将来 `postage` を NULL 許容にする案は `docs/IMPROVEMENTS.md` に記録済み。

### 5-4. 売上の推移が見られなくなる件

指標切替の廃止で、合計行の「売上」額は分かるが売上が伸びているかは見られなくなる（設計案 `6b` も懸念として明記）。

**決定: 収支のみのグラフで進める。薄い売上を重ねる案は今回入れない。**
設計案の「必要になったら収支の背後に薄い売上を重ねる形（切替なし・1 グラフ）で戻せる」
という退避路を IMPROVEMENTS.md に記録済み。

### 5-5. データタブの「期間を指定」の入口

設計案 `6b` の 62 日規則は「期間を指定して長い範囲を選んだとき」を前提にしているが、
`7b` の UI には月バーと期間シート（全期間 / 各月）しかなく、62 日規則が働く場面がない。

**決定: 62 日規則は実装しない。期間指定 UI も作らない。刻みは「月を選択 = 日ごと / 全期間 = 月ごと」の 2 分岐。**
設計案 `7b` の注記（「全期間を選ぶと刻みが『月ごと』に変わり」）と整合する。
SPEC.md §6.2 の `startDate` / `endDate` の自由指定と ◀▶ の平行移動は廃止し、月キー（または全期間）で絞る。

### 5-6. 設定「送料 = 前回の値を使う」

挙動・選択肢・保存先がいずれも未定義。

**決定: 今回は実装しない。設定タブから項目ごと外す。** IMPROVEMENTS.md に記録済み。

### 5-7. 設定「書き出し（CSV）」と「記録の件数」

CSV 書き出しは SPEC-V2 §6.2 でスコープ外とされ未実装だが、`5b` では実装済みの体で描かれている。

**決定: CSV は行だけ置いて「準備中」（非活性）にする。**「入力を減らす（今後）」群と同じ扱い。
実装するときは SPEC-V2 §7-11 のとおり `kind` を 1 列として出す。
**「記録の件数」は実装する**（`repository` に件数取得を 1 本足すだけ）。

### 5-8. 「クリア」の undo

設計案 `2e` は「クリアは入力が空でないときだけ有効。押した直後に『元に戻す』を数秒表示する」としている。
スナックバー相当の部品が現状ない。

**決定: undo は今回実装しない。「入力が空でないときだけ有効」だけ実装する。**
IMPROVEMENTS.md に記録済み。

### 5-9. 各画面の「？」から開くヘルプの実装方式

記録タブ・データタブは設定タブとは別スタックなので、タブをまたぐ push は素直に書けない。

**決定: 各画面の「？」は `HelpScreen` をシート表示し、開く節を props で渡す。**
設定タブの「使いかた」だけが push（`app/(tabs)/settings/help.tsx`）。
`HelpScreen` は開く節を受け取り、その節のアコーディオンを開いた状態で描画する。

### 5-10. 記録タブの検索バー（⌕）の出し方

`8a` はヘッダに ⌕ アイコンだけを描いており、押した後の状態が描かれていない。
現状の `MonthlyRecordListScreen` は常時表示の `SearchBar` で、そのままだと上部が 4 段になる。

**決定: ⌕ タップでヘッダ行を検索フィールドに差し替える。** 常時表示の検索バーは置かない。
上部を 3 段（ヘッダ + 月バー + 合計行）に収めるのが `8a` のねらいのため。

### 5-11. `3c` の伝票カードに仕入価格行がない

設計案 `3c` のレシートは「販売価格 / − 送料 / − 手数料 / ＋ 梱包材・その他 / 純利益」で、仕入価格の行がない。

**決定: デザイン案の描画漏れとして扱う。種別が仕入品のときは「− 仕入価格」を「− 送料」の上に表示する。**
控除が大きい順に並び、伝票の流れが崩れない。不用品では非表示（SPEC-V2 §1.3 / §2.4 のまま）。

### 5-12. `3d` の下部 1 件サマリーの廃止

SPEC-V2 §1.3 は「レコード詳細の下部の 1 件サマリーは種別語を使う」としているが、
`3d` にはその領域がなく、下端は編集・削除の操作列。

**決定: 下部 1 件サマリー（`CareerSummarySection`）は廃止する。**
レシートカードの結果行（「純利益 / 利益」＋額）が同じ役割を果たし、種別語もそこで使われる。
SPEC-V2 §1.3 の当該行は改訂済み。

### 5-13. `3d` の状態表示の重複

メタ行に「売れた」バッジがあり、その下に「売れた状態にする」トグルもある。

**決定: 両方置く。** バッジは状態の表示（一目で分かる位置）、下のカードは状態を変える唯一の手段で、役割が違う。

**後日改訂（§8・案 `15c`）**: トグル自体は廃止し、「売れた」／「出品中に戻す」のボタンと常設の「売れた日」の行に置き換えた。
「両方置く」という上の決定は変わらない。SPEC.md §3.2 の `SaleStatusToggleCard` の記述は §8 が置き換える。

### 5-14. 月バーの矢印の無効化基準

**決定: 初期表示は今月。▶ は今月で無効。◀ はデータのある最古の月で無効。全期間を選んでいる間は両方を無効。**
記録タブ・データタブで同じ規則（`MonthNavBar` に集約）。

---

## 6. SPEC.md / SPEC-V2.md との差分と決定

設計案が SPEC.md / SPEC-V2.md の記述と食い違っていた 11 点。**すべて設計案側を採用**し、
決定を SPEC.md / SPEC-V2.md に反映済み。

### 6-1. 一覧行への種別の常時表示

- **設計案**: `8a` の行のメタ行が「不用品　8/9 販売」、`7b` の内訳行も同様。全行に種別名が常時出る。
- **従来の決定**: SPEC-V2 §5.4「行や一覧に『不用品 / 仕入品』のバッジは置かない」。

**決定: 設計案を採用する。SPEC-V2 §5.4 の「バッジを置かない」決定は破棄する。**
§5.4 の根拠は「金額ラベル（純利益 / 利益）が種別表示を兼ねる」ことだったが、
**§6-2 でその金額ラベル自体を廃止するため前提が崩れる**。
種別を読む手段が一覧から完全に失われるのを避けるため、メタ行に種別名を出す。
SPEC-V2 §5.4 を改訂済み。

### 6-2. 一覧行から金額ラベルを消す

- **設計案**: `8a` の行は右上に金額だけ（`¥1,405`・色で正負）、メタ行右に「経費 ¥395」。
- **従来の決定**: SPEC-V2 §1.3「行の金額ラベル: 不用品 = 純利益: ¥… / 仕入品 = 利益: ¥…」。

**決定: 設計案を採用する。** 行が短くなり一覧性が上がる。種別は §6-1 のメタ行で読み取れる。
SPEC-V2 §1.3 の一覧の表を改訂済み。
`profitLabel(kind)` は記録フォーム・レコード詳細・計算タブで引き続き使うので `labels.ts` に残す。

### 6-3. 出品中の合計「出品価格の合計」

- **設計案**: `8a` の出品中モードの合計行が「出品価格の合計」、行の主金額も出品価格。
- **従来の決定**: SPEC-V2 §5.3 は `salesPrice` を「販売価格」（データタブの集計のみ「売上」）と定めていた。

**決定: 設計案を採用する。** 新語だが、出品中に見るべき指標として必要
（IMPROVEMENTS.md「出品中の合計表示」で挙げていた候補そのもの）。
`labels.ts` に定数を追加し、SPEC-V2 §5.3 のラベル表にも追記済み。

| 対象 | ラベル |
|---|---|
| 出品中レコード 1 件の `salesPrice`（行の主金額） | 出品価格 |
| 出品中の `Σ salesPrice`（合計行） | 出品価格の合計 |
| 出品中レコード 1 件の見込み `netProfit` | 売れたら 約¥…（§5-3 により常に「約」） |

### 6-4. 計算タブの結果側セグメント名

- **設計案**: `3a` は「利益を出す」（種別で変わらない）。
- **従来の決定**: SPEC-V2 §1.3「純利益表示 / 利益表示」。

**決定: 種別に応じて「純利益を出す」/「利益を出す」と出し分ける。**
設計案の語感を採りつつ、不用品で「利益」と表示してしまう問題（SPEC-V2 §5.3 違反）を避ける。
`labels.ts` の `profitTabLabel(kind)` を `` `${profitLabel(kind)}を出す` `` に変更する。

**逆算側は「目標から逆算」（種別で変えない）とする**（本書での派生決定）。
理由: 種別語は直下の入力行ラベル「目標の純利益 / 目標利益」に出るため、セグメント名で重ねる必要がない。
`targetProfitTabLabel(kind)` は定数 `TARGET_TAB_LABEL = '目標から逆算'` に置き換える。

### 6-5. 逆算入力のラベル

- **設計案**: `3a` は `目標の{純利益 or 利益}` で組み立てるため、仕入品では「目標の利益」。
- **従来の決定**: SPEC-V2 §5.3「不用品『目標の純利益』/ 仕入品『目標利益』」（「の」なし）。

**決定: SPEC-V2 §5.3 のまま（`targetProfitLabel(kind)` を使う）。** 変更なし。
画面で文字列を組み立てないこと（§5.3「表示語は 1 箇所に集約する」）。

### 6-6. 記録フォームの種別セレクタの位置

- **設計案**: `3c` は種別セレクタを日付カードの 3 行目（販売日・出品日・種別）に置く。
- **従来の決定**: SPEC-V2 §1.3「『商品情報』セクションの先頭」。§1.5「切替時に仕入価格が消えるのを目視できること」。

**決定: 伝票カード内・商品名の直下（金額の積み上げの直前）に置く**（本書での派生決定）。
設計案の位置（日付カード）では、切替時に消える仕入価格行が伝票カードの中にあり、
スクロール位置によっては画面外になって §1.5 の目視要件を満たせない。
同じカード内に置けば設計案の「伝票 1 枚」の思想も崩れない。SPEC-V2 §1.3 を改訂済み。

### 6-7. 計算タブヘッダの「設定」ボタン（設計案の内部矛盾）

- `3a` のヘッダには「？」と「設定」の 2 ボタンがあるが、確定案の遷移図 `9a` に設定への線はなく、
  設計案 `1e` は「設定をタブへ上げると計算タブのヘッダはボタン 0 個になる」としている。

**決定: 歯車（設定ボタン）は削除する。** 設定はタブから開く。計算タブのヘッダは「？」のみ。
SPEC-V2 §1.3 / §3.3 と SPEC.md §3.2 を改訂済み。

### 6-8. タブ 5 本 → 4 本

- **設計案**: 計算・記録・データ・設定の 4 タブ。ヘルプタブ廃止、出品中/実績を統合、設定を昇格。
- **従来の決定**: SPEC.md §3.1 は 5 タブ。§3.2 は「設定はモーダル。タブは 5 つのまま増やさない」。

**決定: 設計案を採用する。設計案が後発のため優先する。** SPEC.md §3.1 / §3.2 / §3.3 を改訂済み。

### 6-9. 月別詳細画面（`SaleRecordView`）の廃止

- **設計案**: 月別詳細を廃止し、一覧から詳細へ 1 タップ。

**決定: 設計案を採用する。** SPEC.md §3.2 の `SaleRecordView` の項と SPEC-V2 §1.3 の「月別詳細」の表を削除済み。

**「1 系統に統一」は画面の実体（`SaleRecordDetailScreen`）の話**で、ルートの入口はタブごとに置く
（記録タブ・データタブに 1 つずつ）。**タブをまたいで push すると、詳細から戻ったときに
開いたタブではなく別のタブの一覧に着いてしまう**（実機で確認 ── データタブのグラフから開いたのに、
戻ると記録タブにいた）。入口だけを増やして、開いたタブの Stack に積む。
SPEC.md §6.1 の月次グループ集計は、月セクション（`MonthlySummaryCard`）がなくなるため
「選択中の期間 1 本の合計」に読み替える（改訂済み）。

### 6-10. データタブの指標切替・表示単位切替の廃止

- **設計案**: 指標切替（売上金額 / 収支）と表示単位切替（明細 / 日別 / 月別 / 年別）を全廃。

**決定: 設計案を採用する。** SPEC.md §6.2 を改訂済み。
`src/logic/analytics.ts` から `MetricType` / `METRIC_TYPES` / `CHART_UNITS` の `record` `year` /
`RESET_SPAN` / 期間の平行移動が消え、`labels.ts` の `metricLabel` も不要になる。
刻みの決め方は §5-5 の 2 分岐。

### 6-11. データタブ内訳行のラベル

- **設計案**: `7b` の選択日の一覧の行が「¥1,405」＋「不用品　売上 ¥1,800」「経費 ¥395」。
- **従来の決定**: SPEC-V2 §1.3「内訳リストの各行はレコード単位なので種別語（純利益 / 利益）」。

**決定: 設計案を採用する（§6-1 / §6-2 と同じ扱い）。**
記録タブと同じ `RecordRow` を共用する（IMPROVEMENTS.md「データタブの内訳リスト」の指摘も同時に解消）。
SPEC-V2 §1.3 のデータタブの表を改訂済み。

---

## 7. 電卓（MiniCalculator）

設計案は電卓を対象外としていた（「変更なし」と明記）が、**2026-08-10 に別途刷新を確定した**。
本節が §0・§3.4 の「電卓は現状維持」を置き換える。

**ねらい**: 1 回きりの計算しかできない電卓を、**計算メモ**にする。
梱包材のように「箱 120 ＋ 緩衝材 40 ＋ テープ 15」と積み上げてから、その合計だけを欄へ返せるようにする。

式の評価そのもの（`src/logic/calculator.ts` の `evaluateExpression`。SPEC.md 決定 §7-13 の `expr-eval`）と
数値入力のフィルタ（`src/logic/input.ts`）は**変えない**。

### 7.1 表示形式

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 面 | 画面中央のポップオーバー（幅 280px の `Modal`） | **下から出るシート** |
| 見出し | 「電卓」 | **行き先を明示**（例「梱包材の計算」） |
| 閉じる | 右上の ✕ アイコン | **左上の「閉じる」**（文字） |
| 書き戻し | 下端の「この数字を入力する」（幅いっぱいの青ボタン） | **右上の「入れる」**（太字・青） |
| 記号 | `*` `/` | **`×` `÷`**（`*` `/` は画面に出さない） |
| 演算子キーの色 | オレンジ地・白文字 | **青地・白文字**（電卓からオレンジを廃止） |
| クリア | `C` の 1 キー | **`AC`（全消去）と `⌫`（1 手戻す）の 2 キー** |
| キーの形 | 直径 50px の円 | **角丸長方形・高さ 58〜64px**（角丸 12px） |
| `=` | あり | **あり**（§7.1.1。行の中の計算だけを確定する。当初は廃止したが実装後に戻した） |

「オレンジを廃止」は**電卓の中だけ**の話。販売手数料をオレンジで出す規則（§1.1 / §1.3 / §1.4）は変えない。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | シートハンドル | 幅 40px のグラバー（`RecordFormSheet` と同じ） |
| 2 | ヘッダ | 左「閉じる」／中央「{行き先}の計算」／右「入れる」（太字・青。無効時はグレー） |
| 3 | 行の積み上げ | §7.2。**ここだけがスクロールする** |
| 4 | 「＋ 行を足す」と `AC` | 積み上げの末尾の 1 行。左が青リンク（`3c` の「＋ 梱包材・その他」と同じ見た目）、右が `AC`（§7.1.1） |
| 5 | 合計行 | 左に「合計」、右に合計額（24px 太字・`tabular-nums`。負は赤・§7.4） |
| 6 | キーパッド | 4 列 × 4 行。下端に固定 |

**キーパッド**

| | 1 列 | 2 列 | 3 列 | 4 列 |
|---|---|---|---|---|
| 1 行 | `7` | `8` | `9` | `÷` |
| 2 行 | `4` | `5` | `6` | `×` |
| 3 行 | `1` | `2` | `3` | `−` |
| 4 行 | `0` | `⌫` | `=` | `＋` |

- 従来の 4×4 の並びを保つ（数字の位置が変わらない）。
- 色: 数字 = カード地・黒文字 ／ `÷ × − ＋ =` = 青地・白文字 ／ `⌫` = カード地・**グレー文字**。
- **小数点キーは従来どおり置かない。** 金額は整数で入れる。割り算で出た小数は `evaluateExpression` の
  規則どおり小数第 1 位まで表示する。

#### 7.1.1 `=` キーと `AC` の位置（実装後の追補）

行の結果は右端に常に出るので `=` は本来なくても足りるが、**電卓では `=` を押す手が先に出る**
（押しても何も起きないと壊れて見える）という指摘を受けて戻した。役割は §7.2 のとおり
**行の中の計算を確定するだけ**で、行は積まない。

`=` を足すとキーが 17 個になり 4×4（16 枠）に収まらないため、`AC` をキーパッドから出して
**「＋ 行を足す」と同じ行の右端**へ移した（採用案 A）。

- `AC` は行の並び全体に効く操作なので、1 行ずつに効く `⌫` とは別の場所にある方が近い。
- `0` の隣から外れるので、§7.1 が文字色で避けようとしていた押し間違いも起きなくなる。
- `=` は `＋` の隣に置く。**「この行を計算する（`=`）」と「行を積む（`＋`）」の違いが並びで読める。**

### 7.2 計算メモ型の積み上げ

**1 行の構成**（左から）

| 列 | 内容 | 幅 |
|---|---|---|
| 記号 | `＋` / `−`。**その行を合計へどう足すか**を表す | 固定 |
| 品名 | 任意。**現時点では常に空**（§7.5） | 空なら **0**（式が左端から始まる） |
| 式 | `1500 ÷ 100`、`3 × 25` など。四則演算が使える | 可変 |
| 結果 | その行の値（右寄せ・`tabular-nums`） | 固定 |

- `1500 ÷ 100` も `3 × 25` も **1 行**に収まる。結果は右端に常に出るので、`=` を押さなくても読める。
- **行内の `×` `÷` はその行の中の計算**で、合計への足し方には影響しない。
  合計への足し方を決めるのは行頭の記号だけ。
- 1 行目の記号も `＋` を出す（列がそろい、後から `−` 行が積まれても式の左端が動かない。**派生決定**）。

**行の積み方**

| 操作 | 起きること |
|---|---|
| `＋` | 編集中の行を確定して積み、次の行を `＋` で始める |
| `−` | 編集中の行を確定して積み、次の行を **`−`** で始める（その行は合計から引かれる） |
| 「＋ 行を足す」 | `＋` キーと同じ。積み上げの側からも行を足せることを示すために置く |
| `×` `÷` | 行は積まれない。編集中の行の式に続く |
| `=` | 行は積まれない。**編集中の行の中だけ**で計算を確定し、式を結果に置き換える（§7.1.1） |

- 例: `120` → `＋` → `40` → `＋` → `15` で 3 行が積まれ、合計 175。
- 例: `1500` → `−` → `300` で「＋ 1500」「− 300」の 2 行になり、合計 1200。
- 例: `2 × 3` → `=` で行が「6」になる（行は確定していないので、続けて `× 2` と打てる）。
  そこで `＋` を押せば「＋ 6」が積まれ、次の行が始まる。
- **編集中の行（積み上げの最終行）の結果も合計に含める。**
  `＋` をまだ押していない値が合計から漏れると、見えている数字と合計が食い違うため（**派生決定**）。
- 式が空のまま `＋` `−` を押したときは、行を積まずに**次の行の記号だけを差し替える**（空行は積まない）。
- 演算子で終わる式は評価しない（`evaluateExpression` の現行ガード）。その行の結果は演算子を押す前の値のまま。

**開いたときの状態**

| 呼び出し元の欄 | 開いたときの状態 |
|---|---|
| 空 または `0` | 行なし・編集中の行も空（現行の `targetText === '0'` の扱いを踏襲） |
| 値が入っている | その値を**編集中の行の式**に入れた状態から始める |

### 7.3 訂正

| 操作 | 対象 | 挙動 |
|---|---|---|
| 行を左スワイプ →「削除」 | 積んだ行 | 1 行ずつ削除。確認は挟まない。**合計は即座に再計算** |
| `⌫` | 編集中の行 | 末尾 1 文字を消す。編集中の行が空のときは**直前に積んだ行を編集中に戻す**（＝行を積んだ操作の取り消し） |
| `AC` | 全体 | 積んだ行・編集中の行をすべて消して初期状態に戻す。確認は挟まない |

- **行の編集機能は持たない。** 積んだ行をタップしても何も起きない。
  削除して入れ直す方が手数が少なく、行内キャレットの実装も要らないため。
- 左スワイプは `RecordListScreen` の `SwipeToDeleteRow`（`ReanimatedSwipeable`）と同じ形にする
  （赤地・白文字の「削除」・`rightThreshold={40}`）。
- 編集中の行はスワイプの対象にしない。`⌫` `AC` で消せるので、同じことをする経路を 2 つ作らない（**派生決定**）。
- `AC` に確認アラートを置かないのは、積み上げがそもそもシートを閉じれば消えるもの（§7.4）で、
  失われるものが小さいため。

### 7.4 書き戻し

- 「入れる」で**合計だけ**が呼び出し元の欄に入り、シートが閉じる。行は渡さない。
- 積み上げた行は**保存しない。シートを閉じると消える**（「閉じる」でも「入れる」でも同じ）。
  次に開いたときは §7.2「開いたときの状態」から始まる。
- 書き戻しの経路は現行どおり `NumericField` の `onSubmit` → `sanitizeNumericInput`。

**「入れる」の有効・無効**

| 合計 | 表示 | 「入れる」 |
|---|---|---|
| 0 以上 | 通常（黒） | ○ |
| **負** | **赤** | **×（押せない）** |
| 行が 1 つもなく編集中の行も空 | 「0」 | ×（入るものがない。**派生決定**） |

- 負を弾くのは、**書き戻し先の数値欄がマイナスを受け付けない**ため。
  `sanitizeNumericInput` は `-` を落とすので、`-300` を渡すと `300` になり符号が黙って反転する。
- **`src/logic/input.ts` のフィルタは変更しない。** 欄の側でマイナスを許すと、SPEC.md §5.1 の許容形式・
  DB の各金額列・`profit.ts` の前提まで波及するため、電卓の側で止める。
- 無効の理由は合計行の下に 1 行で出す（例:「合計がマイナスのままでは入れられません」）。
  ボタンがグレーなだけでは理由が分からないため。

### 7.5 品名の列

- **現時点では常に空。** 品名を入れる手段は用意しない。空のときは列の幅を 0 にし、式が左端から始まる。
- 将来「梱包材の登録」（§1.6-3「入力を減らす（今後）」）を実装したら、
  **選んだ品名がそのまま左列に入る**想定。
- そのため行のデータ構造には最初から `name` を持たせ、今回は常に空文字を入れる（後から列を足さない）。
- 品名が入っても行の高さは変えない（1 行 1 段のまま）。

### 7.6 実装メモ

| ファイル | 内容 |
|---|---|
| `src/components/MiniCalculator.tsx` | シート化（器は `SheetModal`・§3.5）・行の積み上げ・キーパッド刷新 |
| `src/logic/calcMemo.ts`（仮・新規） | 行モデル（`{ sign, name, expression }`）と合計の算出。純粋関数＋単体テスト |
| `src/logic/calculator.ts` | `×` `÷` を受け付ける（評価の直前に `*` `/` へ置き換える）。`isCalculatorOperator` に `×` `÷` を追加 |
| `src/logic/labels.ts` | 見出し「{行き先}の計算」を組み立てる語を追加（画面で文字列を組み立てない・§0） |
| `src/components/NumericField.tsx` | 電卓へ行き先のラベルを渡すのみ。行の形・電卓ボタンの位置は変えない |
| `src/logic/input.ts` | **変更しない**（§7.4） |

- **記号の変換は `calculator.ts` に閉じる。** 行の式は表示どおり（`×` `÷`）で保持し、画面側で置換しない。
- 合計は**各行の結果（表示されている値）を符号どおりに足した値**とする。
  丸め前の値で足すと表示の合計と食い違うことがあるため、見えているものと一致する方を採る（**派生決定**）。
- 見出しは `NumericField` の `label` から作る（「梱包材」→「梱包材の計算」、「送料」→「送料の計算」）。
  逆算モードの「目標の純利益」なども同じ規則。
- 電卓ボタンを持つ数値欄すべてから同じシートが開く（計算タブ・記録フォーム。`NumericField` 経由）。
  欄ごとの出し分けは見出しの語だけ。

---

## 8. 出品中 ⇄ 売れた の切り替え（案 `15c`）

**設計案ターン 15 で確定した後発の決定。** 設計案の当初案（`3d` の売却トグル、`3c` の状態切替リンク）を置き換える。
本節は §1.3-3 / §1.4-5 / §5-13 に優先する。

**ターン 16 の追補**: 実装後の見直しで、レコード詳細の**状態ボタンの見た目**（案 `16a`。§8.9）と
**売れた日のピッカーの形式**（案 `16d`。§8.10）を差し替えた。
どちらも §8 の方針（押した時点で確定・直せる場所を残す）は変えず、その見せ方だけを変える。
§8.9 / §8.10 は §8.1 / §8.2 / §8.4 / §8.5 の該当箇所に優先する。

**ターン 17 / 18 の追補**: 実利用者の指摘（「ホイールは通り過ぎるので使いたくない」）を受け、
**アプリ内のホイール型ピッカーを全廃**し、日付の選び方を**行内のチップ＋カレンダーシート**（案 `17b`）、
カレンダーの月移動を**年月グリッド**（案 `18a`）に確定した。§8.10 を全面的に差し替えている。
これは売れた日だけの話ではなく、**出品日を含むアプリのすべての日付欄**に適用される。

**解く問題**: 現状のトグルは ON にすると `saleDate = 今日` を無条件で入れる。
数日前に売れた記録を後からつけると、**実際と違う日付が黙って入る**。
かといって「売れた」を押すたびに日付選択を挟むと、当日に記録する多数派の手数が増える。

**方針**: **押した時点で今日として確定し（追加タップ 0）、直せる場所を画面に残し続ける。**
訂正を先払いさせず、後からいつでも直せるようにする。

### 8.1 出品中 → 売れた

- トグルをやめ、**「✓ 売れた」ボタン**にする（見た目とレイアウトは §8.9）。
- 押した時点で `saleDate = 今日` として**即座に保存**する（追加タップ 0。詳細画面の場合。§8.6）。
- 押すとカードは状態カード（バッジ側）の表示に切り替わり、**その下に売れた日の行が開く**。
- 確認ダイアログは出さない。取り消しは §8.3 のバー、訂正は §8.2 の日付行が受け持つ。

### 8.2 売れた日の行（常設）

```
売れた日   今日（2026/08/10）  ▸
```

- **売れた記録である限り常設**。押した直後だけでなく、いつ開いても同じ位置にある。
- 行の中の**「今日・昨日・一昨日」のチップ**で 1 タップで直せる。それ以外の日付は
  「日付 ▸」から**カレンダーシート**を開く（§8.10。制約は §8.5）。**ホイールは使わない。**
- 値の表示は既存規則どおり ── 当日は「今日（2026/08/10）」、それ以外は日付そのもの
  （`todayDateText`。§1.3-12 と同じ）。
- 出品中の間はこの行を出さない（出品中に販売日はない。SPEC.md §3.2）。

### 8.3 押した直後の 2 つの合図

「売れた」を押した直後だけ、次の 2 つを同時に出す。**役割が違うので両方置く。**

| 合図 | 位置 | 役割 | 消えるか |
|---|---|---|---|
| 薄い青の下地 | 売れた日の行 | **どこを直せばいいか**を指す | 数秒後に通常表示へ戻る |
| 「売れた記録にしました ／ 元に戻す」バー | 画面下部 | **取り消し**の口を出す | 数秒で消える |

- **バーが消えても日付行は残る。** バーは取り消しの猶予、日付行は恒久的な訂正口。
  「消えるもの」と「残るもの」を混同しないこと。
- 「元に戻す」は `saleDate = null` に戻して保存する。**§8.4 の確認ダイアログは出さない**
  （直前の操作の取り消しであり、確認済みの意思のため）。
- ハイライトとバーの表示時間は**同じ 1 つの定数**にする。**暫定 4 秒**（**派生決定**。数値は実装時に調整可）。
- 日付行を先に押した場合はハイライトを即座に解除する（役目を終えたため）。
- 読み上げ: ボタン押下時に「売れた記録にしました」をアナウンスする。
  バーは数秒で消えるので、**バーだけに情報を載せない**。

### 8.4 売れた → 出品中

- **「出品中に戻す」ボタン**（**枠線のみの控えめな見た目**。§8.9）。
- 押すと確認を **1 枚だけ**出す ── 「販売日 8/10 が消えます。戻しますか？」
  （日付は M/d。メタ行と同じ形式）
- 実行で `saleDate = null`。取り消しの口は出さない（確認を先に取っているため）。
- 逆方向だけ確認を挟むのは非対称だが意図どおり ── **こちらは入力済みの日付が消える破壊的操作**で、
  順方向（今日を入れるだけ・すぐ直せる）とは重さが違う。

### 8.5 日付の制約

売れた日のピッカーに次の 2 つを課す。**選べない日はピッカー側で選択不可にする**（選ばせてから叱らない）。
ただし**選択肢そのものを消すのではなく、出したうえで選べないと分かる形にする** ── 見せ方は §8.10。

| 制約 | 理由 |
|---|---|
| **今日より後を選べない** | まだ起きていない販売日は入らない |
| **出品日より前を選べない** | 出品前に売れることはなく、経過日数（§5-2）が負になる |

- 実質の選択範囲は `[saleStartDate, 今日]`。この範囲が 1 日しかない（当日出品・当日販売）場合も同じ扱い。
- 出品日を後から未来に動かすなど、既存データが制約を破る場合は**保存済みの値をそのまま表示する**
  （表示を偽らない）。次にピッカーを開いたときに範囲内へ寄せる。
- **`saleStartDate > 今日`（出品日が未来）のときに「売れた」を押した場合は
  `saleDate = saleStartDate` を入れる**（**派生決定**。今日を入れると自分の制約を破るため）。
  この場合、日付行は「今日（…）」ではなく実日付を出す。

### 8.6 保存のタイミング

既存方針を変えない。**画面によって違う。**

| 画面 | 書き込み |
|---|---|
| レコード詳細（`3d`） | **押した時点で保存**（現状どおり） |
| 記録フォーム（`3c`） | **「保存」を押すまで書き込まない**（既存方針のまま） |

これに伴い、**記録フォームでは確認ダイアログも undo バーも出さない**（**派生決定**）。
まだ何も書かれていないので取り消す対象がなく、戻したければ見出し行をもう一度押せばよい。
ハイライト（§8.3 の薄い青）は**両方の画面で出す**（開いた行の位置を指すため）。

### 8.7 画面ごとの適用

**レコード詳細（`3d`）— 状態カード**

- §1.4-5 の「売却トグル」を、状態に応じた 1 個のボタンに置き換える。

| 状態 | カードの中身 | その下 |
|---|---|---|
| 出品中 | 「売れた」ボタン | なし |
| 売れた | 「出品中に戻す」ボタン | 売れた日の行（常設） |

- メタ行の「売れた」バッジは残す（表示と操作で役割が違う。§5-13 の判断は維持）。

**記録フォーム（`3c`）— 見出し行**

- 見出し行の切替リンク（§1.3-3）はそのまま**状態を切り替える口**として使う。
- 切り替えると、**伝票カードの下の日付カード（§1.3-12）の中で売れた日の行がその場で開く／閉じる**。
  別のシートや画面には飛ばさない。
- 「売れた記録にする」で開いた行の初期値は今日（§8.5 の制約下）。
- 「出品中にする」では販売日の行を閉じる。値は保持せず、保存時に `saleDate = null`。

### 8.8 実装メモ

| ファイル | 内容 |
|---|---|
| `src/components/RecordDetailSections.tsx` | 売却トグル（`SaleStatusToggleCard` 相当）→ 状態別 1 ボタン＋売れた日の行 |
| `src/components/DateField.tsx` | 選択できる範囲（`minDate` / `maxDate`）を受け取る（§8.5）。範囲の伝え方は §8.10（**ホイールから外す実装は廃止**） |
| `src/components/UndoBar.tsx`（仮・新規） | 画面下部のバー（本文＋「元に戻す」）。数秒で自動的に消える |
| `src/screens/SaleRecordDetailScreen.tsx` | 即時保存・undo・確認ダイアログの配線（§8.6） |
| `src/screens/RecordFormSheet.tsx` | 見出し行の切替で日付行を開閉。保存は「保存」時のみ（§8.6） |
| `src/logic/labels.ts` | 下記の語を追加・差し替え（画面で文字列を組み立てない・§0） |

**`labels.ts` の変更**

| 語 | 扱い |
|---|---|
| `MARK_AS_SOLD_LABEL`（「売れた状態にする」） | **廃止**。トグルがなくなるため |
| 「売れた」（ボタン） | 追加。`SOLD_BADGE_LABEL` とは用途が違うので別の定数にする |
| 「出品中に戻す」（ボタン） | 追加 |
| 「売れた日」（行のラベル） | 追加。`SOLD_DATE_FIELD_LABEL`（「販売日」）とは別 ── 行の語と入力欄の語を揃えない |
| 「販売日 {M/d} が消えます。戻しますか？」 | 追加（日付を受け取る関数） |
| 「売れた記録にしました」／「元に戻す」 | 追加 |

- **`saleDate` の型と DB スキーマは変えない**（`Date | null` のまま。SPEC.md §2）。本節は入れ方だけの変更。
- §5-8（計算タブ「クリア」の undo は今回実装しない）は**据え置き**。
  ただし `UndoBar` ができるので、後で実装するときはこれを使い回す（IMPROVEMENTS.md に記録済みの項目）。

**ターン 16 の追補ぶん（§8.9 / §8.10。実装済み）**

| ファイル | 内容 |
|---|---|
| `src/components/RecordDetailSections.tsx` | `SaleStatusCard` を左（状態バッジ）／右（ボタン）の横並びに組み替える。ボタンは実行側＝塗り・戻す側＝枠線（§8.9） |
| `src/components/CalendarPicker.tsx`（新規） | カレンダー形式のピッカー。月の全日を出し、範囲外は淡く表示。今日に印・出品日に旗・選べない理由の一行。週の始まりは日曜固定（§8.10.2） |
| `src/logic/labels.ts` | 選べない理由の一行を追加。状態バッジは既存の `LISTING_STATUS_LABEL` / `SOLD_RECORDS_LABEL` をそのまま使う（補足行は §8.9 の決定により置かない） |

**ターン 17 / 18 の追補ぶん（§8.10。実装済み）**

| ファイル | 内容 |
|---|---|
| `src/components/DateField.tsx` | ホイール（`DatePickerSheet`）を**削除**。行に「今日・昨日・一昨日」のチップを常設し（範囲外は淡色・押せない）、「日付 ▸」で `CalendarPicker` を開く（§8.10.1） |
| `src/components/CalendarPicker.tsx` | 上部にチップ、年月見出しをボタン化して年月グリッドを開く。`◀ ▶` は範囲外の月へ行けないとき無効（§8.10.2 / §8.10.3） |
| `src/components/WheelColumn.tsx` | **廃止**。`DateField` の差し替えで参照がなくなる（`MonthPickerSheet` はすでに未参照。§8.10.5） |
| `src/components/MonthPickerSheet.tsx` | **廃止**。期間シート（§1.2 の月グリッド）に置き換わり済みで、すでにどこからも使われていない |
| `src/logic/calendar.ts`（新規） | 盤面の組み立て（日曜始まりの週配列・範囲判定・月送りの可否）と年月グリッドの候補。純粋関数＋単体テスト |
| `src/logic/saleDate.ts`（新規） | 範囲（`[出品日, 今日]`）の算出と範囲内への寄せ（§8.5）。純粋関数＋単体テスト |
| `src/logic/labels.ts` | チップの語（「今日」「昨日」「一昨日」）・曜日見出し・選べない理由の一行を追加（画面で組み立てない。§0） |

- `SOLD_DATE_ROW_LABEL`（「売れた日」）・`MARK_AS_SOLD_BUTTON_LABEL`・`REVERT_TO_LISTING_BUTTON_LABEL` は**そのまま**。
  `16a` で変わるのはボタンの見た目と周りの配置で、語ではない（チェックアイコンは文字列に含めない）。
- 補足行「8/9 に出品・1日経過」は**実装しない**（§8.9 の決定）。メタ行の `recordTimelineText` と
  同じ事実を 2 度読ませるため。状態カードが持つのはバッジとボタンだけになる。

### 8.9 状態ボタンの見た目とレイアウト（案 `16a`）

**現在の実装を変更する。** 現状は緑一色・横幅いっぱいの 1 行カードで、状態の説明を持たない。

**解く問題**: 幅いっぱいの塗りボタンは、押す前に**何の状態に対する操作なのか**を示せない。
出品中と売れた記録でボタンの語だけが入れ替わり、いま何なのかは上のメタ行まで目を戻さないと分からない。

**方針**: **幅いっぱいをやめ、状態の説明と対で横に並べる。**

```
[ 出品中 ]                        ┌──────────┐
                                  │ ✓ 売れた │
                                  └──────────┘
```

| 位置 | 中身 |
|---|---|
| 左 | **状態バッジ**（「出品中」／「売れた記録」）。ボタンの主語 |
| 右 | ボタン |

当初は左のバッジの下に**補足行**（「8/9 に出品・1日経過」）を置く形にしていたが、
下記「メタ行のバッジとの関係」の決定により**補足行は置かない**（実装済み）。

- 出品中の記録 ── **「✓ 売れた」**。**緑の塗り・チェックアイコン付き**。
- 売れた記録 ── **「出品中に戻す」**。**枠線のみの控えめな見た目**（塗らない）。
- **実行側は塗り、戻す側は枠線**とすることで、主たる操作を視覚的に区別する。
  この差は §8.4 の「順方向と逆方向は重さが違う」という判断を見た目に写したもので、
  確認ダイアログの有無（順方向はなし・逆方向はあり）と同じ非対称を指している。
- 語は `labels.ts` に置く（画面で組み立てない。§0）。

**メタ行のバッジとの関係（決定済み）**

§5-13 で「メタ行の状態バッジは残す（表示と操作で役割が違う）」と決めているが、
`16a` の当初案の状態カードは**自前の状態バッジと日付の補足行を持つ**ため、同一画面に
状態バッジが 2 つ・出品日と経過日数が 2 か所という重複が生じていた。

**実機で見て次のように決めた ──「補足行を落とし、バッジは両方残す」。**

| 対象 | 扱い | 理由 |
|---|---|---|
| メタ行のバッジ | **残す** | §5-13 の決定を維持。記録そのものの見出しで、種別・日付と 1 組 |
| 状態カードのバッジ | **残す** | **ボタンの主語**。左のバッジと右のボタンが対になっているのが `16a` の要点で、落とすと `16a` が直そうとした「語だけのボタン」に戻る |
| 状態カードの補足行 | **廃止** | メタ行から種別を抜いただけの同じ事実。短いレコードでは両方が同時に画面へ入り、同じ日付を 2 度読ませていた |

- 語（「出品中」／「売れた記録」）がメタ行と重なるが、**指しているものが違う** ──
  メタ行は記録の状態、状態カードは操作の対象。§8.8 の「表示と操作で役割が違う」と同じ切り分け。
- これに伴い `labels.ts` の `statusCardTimelineText`（補足行の語）は**廃止**した。
  日数規則（出品日起算・当日 0 日。§5-2）を読む場所はメタ行の `recordTimelineText` だけになる。

**不採用: スワイプ確定（案 `16c`）**

「売れた」を**スワイプで確定させる**案は採らない。理由:

| 理由 | 内容 |
|---|---|
| 手数の配分が逆 | 「今日売れた」が大半のため、1 タップで済む操作を重くするのは配分が逆（§8 の方針そのもの） |
| 防ぐべき損失ではない | 誤タップは §8.3 の undo バーで 1 タップ取り消せる。取り返しがつく操作に予防コストを払わせない |
| 操作の意味が衝突する | 記録一覧で**左スワイプを削除に使っている**（§1.2）。同じ操作で違う結果が起きると混乱する |
| 予約する先が決まっている | **スワイプは取り返しのつかない削除にのみ予約する** |

### 8.10 日付の選び方 ── 行内チップとカレンダー（案 `17b` + `18a`）

**ターン 16（案 `16d`）で決めたカレンダー化を、ターン 17 / 18 で全面的に置き換えた確定版。**
本項は §8.2 / §8.5 の「ピッカー」の具体形を定め、`DateField` に関する §8.8 の記述に優先する。

**解く問題（その 1・ホイール）**: 実利用者から **「ホイールは通り過ぎるので使いたくない」** という指摘があった。
ホイールは目標の値を止めて選ぶ操作で、行き過ぎ・戻しが常に起きる。日付は**候補が少数に偏る**
（大半は今日・昨日）にもかかわらず、その多数派にまで回す操作を強いていた。

**解く問題（その 2・欠落した選択肢）**: 旧実装は範囲外の年・月・日を**ホイールから外して**いたため、
利用者が**「過去に入力した内容しか出てこない」と誤解した**（実際は `[出品日, 今日]` の範囲外を除いていただけ）。
回しても出てこない欠落は、**制約なのか不具合なのか、あるいは入力履歴なのかが区別できない。**

**方針**:

1. **多数派の日付は行の中で 1 タップで決める。** シートを開かせない。
2. **それ以外を選ぶときだけ盤面（カレンダー）を出す。**
3. **選べない選択肢は、消すのではなく、選べないと分かる形で出す。**
4. **ホイールは使わない。** アプリ内のホイール型ピッカーを全廃する。

#### 8.10.1 日付行（記録フォーム・レコード詳細）

```
販売日                        今日（2026/08/10） ▸
[ 今日 ] [ 昨日 ] [ 一昨日 ]
出品日（8/10）より前は選べません
```

- **「今日・昨日・一昨日」のチップを行の中に常時表示**し、**シートを開かず 1 タップで選べる**。
  押した時点で値が決まる（確定操作は挟まない）。
- それ以外の日付を選ぶときだけ、右の **値のボタンからカレンダーシートを開く**。
  ボタンには**現在の値**を出す（「日付 ▸」の固定文言にはしない）── チップにない日付
  （例 2026/07/03）を選んだとき、その値が読める場所が行の中に他にないため。
  ラベル＋値の 1 行はそのまま残し、チップはその下に段を足す（§8.10.5。1 行に詰めると端末幅に収まらない）。
- チップの並びは **今日 → 昨日 → 一昨日 の固定**。選択中のチップは選択状態を示す。
- **範囲外のチップは淡色にして押せなくする**（消さない。方針 3）。
  - **販売日**: 出品日より前になるチップを淡色にする（当日出品なら「昨日」「一昨日」が落ちる）。
  - **出品日**: この制限はない。過去はどこまでも選べ、**未来のみ不可**（今日より後のチップは存在しないので、
    実際にはチップが落ちることはない）。
- **淡色のチップがあるときは、その理由をチップのすぐ下に一行で出す。**
  淡くするところで止めると**押せないのが不具合に見える** ── 当日出品の記録で「昨日」「一昨日」が
  落ちるのをバグと受け取られた実例がある。§8.10.5 の「淡色と理由の一行で 1 組」は
  カレンダーだけの話ではなく、**チップにも同じだけかかる**。
  - 語はカレンダーの一行とは**別**（「出品日（8/9）より前は選べません」）。
    行とシートで**淡くなっているものが違う**ため ── 盤面には未来の日も並ぶが、チップは
    今日・昨日・一昨日の 3 つしかなく、落ちるのは必ず下限（出品日）側だけ。行に「今日より後は選べません」と
    書くと、対応する淡いチップが画面になく、在りもしない選択肢を探させることになる。
  - **落ちたチップが 1 つもない行には出さない**（出品日の行には出ない）。無い制約を書くと同じ探し物が起きる。
  - 出品日が未来のときだけ 3 つとも落ちるので、カレンダーと同じ「出品日（8/20）だけが選べます」に寄せる
    （§8.5 派生決定 3）。
  - **当日出品（選べるのが「今日」だけ）でも言い方は変えない。**「出品日（8/10）より前は選べません」で
    2 つの淡色を説明できており、状態ごとに文を作り分けると規則のほうが読めなくなる。
- チップで選んだ値も日付そのもの（時刻は元の値を引き継ぐ）。表示は既存規則どおり
  ── 当日は「今日（2026/08/10）」、それ以外は日付そのもの（§1.3-12 / §8.2）。

#### 8.10.2 カレンダーシート（出品日・売れた日で共通）

**同じ部品を使い、選べる範囲だけが違う。** 欄ごとに別の見た目を作らない。

**構成（上から）**

| # | 要素 | 内容 |
|---|---|---|
| 1 | 見出し | 欄の名（「出品日」／「売れた日」） |
| 2 | チップ | 「今日・昨日・一昨日」。**フォームの行と同じ並び・同じ淡色規則**（§8.10.1） |
| 3 | 年月見出し | `◀ 2026年8月 ▾ ▶`。**見出し自体がボタン**で、押すと年月グリッド（§8.10.3）が開く |
| 4 | 盤面 | 月のカレンダー。**週の始まりは日曜固定**。月の全日を出す |
| 5 | 理由の一行 | **選べない理由を一行で明示する**（例: 出品日より前は選べない旨） |

- **選べない日は淡色**にして盤面に残す（消さない）。押しても何も起きない。
- **今日に印**、**出品日に小さな旗**を出す（売れた日のシート）。範囲の端が盤面の上で見えるようにする。
- **◀ ▶ で月送り**。**範囲外の月へは行けない ── 矢印を無効にする**（押せるのに何も起きない、にしない）。
  - **出品日**: 明日以降のみ淡色。**◀ は常に有効**（過去に下限がない）。
    今月を表示しているとき ▶ は無効。
  - **売れた日**: **出品日より前と明日以降が淡色**。**◀ は出品日の月まで来ると無効**になり、
    ▶ は今日の月で無効になる。
- 日を押した時点で値が決まり、シートは閉じる。**「決定」は持たない**（閉じるボタンは閉じるだけ）。
- 週の始まりをロケールで振らないのは、本アプリが**日本語のみ・日本の利用者向け**のため（§0）。
- 保存済みの値が範囲外の場合の扱いは §8.5 のまま（**表示は偽らず**、開いたときに範囲内へ寄せる）。

#### 8.10.3 年月グリッド

**◀ の連打を避けるための導線。** 数か月前・去年の日付へ、月送りを繰り返さずに移る。

- 年月見出し（§8.10.2 の 3）を押すと開く。
- **年ごとに月を 4 列グリッドで表示**（1〜12 月を 4 列 × 3 行）。
  **期間選択シート（§1.2 の月グリッド）と同じ形式**にする ── 同じ「月を選ぶ」操作を 2 通りの形で覚えさせない。
- **月を押すとカレンダーに戻る**（その月の盤面を出す）。日はまだ決まっていない。
- **範囲外の月は淡色で選べない**（盤面と同じ規則。消さない）。
- 年の並びと表示範囲は期間選択シートに揃える（降順。**新しい年が上**）。

#### 8.10.4 適用範囲

| 欄 | 画面 | 選べる範囲 |
|---|---|---|
| 出品日 | 記録フォーム（§1.3-12） | `(…, 今日]` ── **未来のみ不可** |
| 販売日 | 記録フォーム（§1.3-12） | `[出品日, 今日]`（§8.5） |
| 売れた日 | レコード詳細（§8.2） | `[出品日, 今日]`（§8.5） |

**いずれも同じカレンダー部品を使い、選べない範囲だけが異なる。**
範囲の指定は `minDate` / `maxDate` の 2 つで済み、欄ごとの分岐を部品の中に持ち込まない。

#### 8.10.5 廃止

| 対象 | 扱い |
|---|---|
| `DateField` のホイール型ピッカー（`DatePickerSheet`） | **廃止**。行の見た目（ラベル＋値）は残し、開く先をカレンダーに差し替えたうえでチップを行に足す |
| `WheelColumn` | `DateField` の廃止で参照がなくなる（`MonthPickerSheet` は §1.2 の期間シートに置き換わり済みで、すでに未参照）。**アプリ内にホイールを残さない** |
| 期間選択シート（§1.2 の月グリッド） | **影響なし**。もともと月グリッド形式で、ホイールを使っていない |

**方針として残すこと**: **選べない選択肢は、消すのではなく、選べないと分かる形で出す。**
消すと利用者は欠落の理由を自分で埋めるしかなく、たいてい間違った理由を埋める。
淡色にしたうえで、**なぜ選べないかを一行で名指しする**ところまでが 1 組。

**この 1 組は盤面だけでなく行のチップにも適用する**（§8.10.1）。実装時に行へ理由を出し忘れ、
当日出品の記録で淡色になった「昨日」「一昨日」を不具合と受け取られた ── 淡色だけを移植して
理由の一行を落とすと、**制約が不具合に化ける**。

---

## 9. 参照

- `docs/SPEC.md` — 移植元の全体仕様（§2 計算式、§5 バリデーション、§6 集計）
- `docs/SPEC-V2.md` — レコード種別と表示語の決定（**§5.3 確定ラベル表**）
- `docs/UI-INVENTORY.md` — 現行 UI の棚卸し
- `docs/IMPROVEMENTS.md` — 改善候補の一覧（本書で見送った項目の記録先）
- `src/logic/labels.ts` — §5.3 の表示語の実装（画面で文字列を組み立てないこと）
