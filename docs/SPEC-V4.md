# タグによる分類と絞り込み 仕様 — SPEC v4

記録に「洋服」「食器」などの**タグ**を付け、後から絞り込めるようにするための仕様。
`docs/IMPROVEMENTS.md`「タグによる分類」（未着手）と「絞り込みの拡張 / 販売サイトでの絞り込み」
（同項が「実装するなら絞り込み UI をまとめて設計する必要がある」と書いていた）を、
**1 つの絞り込み UI にまとめて**確定させる。
実装前に確定させるための文書であり、この時点ではコードは変更しない。

前提とする既存文書:

| 文書 | 位置づけ | 本書での参照 |
|---|---|---|
| `docs/SPEC.md` | 移植元の確定仕様（計算式・スキーマ・画面） | `SPEC §x.y` |
| `docs/SPEC-V2.md` | レコード種別（不用品 / 仕入品） | `SPEC-V2 §x.y` |
| `docs/UI-SPEC.md` | UI 全面改訂。**UI に関する記述は本書より優先度が高い既定** | `UI-SPEC §x.y` |
| `docs/SPEC-V3.md` | プリセット（販売サイト / 送料 / 梱包材）と CSV | `SPEC-V3 §x.y` |
| `docs/IMPROVEMENTS.md` | 見送り項目の記録 | `IMPROVEMENTS` |

本書内の参照は `§x.y` と表記する。
設計（案 `30a` = 入力 / 案 `30b` = 絞り込み / 案 `21d` = 解除バー）は Claude Design で確定済み。

**本書の全項目は 2026-08-11 に確定済み。** §9 に決定の一覧（旧「要確認」14 件）を置く。
未確定の項目は残っていない（§9-15 のみ実装時の調査項目）。
**§8 で挙げた既存文書との衝突は、同日に各仕様書へ反映済み**（UI-SPEC §1.2 / §1.4 / §6-6 /
並び替えシート、SPEC-V3 §1.4 / §1.5.1 / §5.3、IMPROVEMENTS の 2 項）。

---

## 0. 前提と方針

- **タグは「入力を楽にする」機能ではなく「記録の中身を増やす」機能**（IMPROVEMENTS の分類そのまま）。
  プリセット（SPEC-V3）は選ぶと**欄に数値が入るだけ**で記録には残らないが、
  タグは**記録に残り、後から絞り込みの対象になる**。この違いがデータモデル（§1）と
  設定画面の置き場所（§2）の両方を決める。
- **タグは記録を参照される側になる。** プリセットが物理削除で済んだのは
  「レコードが id を参照しない」（SPEC-V3 §1.1 / §1.5）からで、タグにはその前提がない。
  よって**プリセットと同じテーブルには入れない**（§1.1）。
- **絞り込みは 1 か所にまとめる。** 種別（SPEC-V2 §4.2）・販売サイト（SPEC-V3 §1.5.1 で先送り）・
  タグの 3 つを、合計行のチップを増やすのではなく**1 枚のシート**に入れる（案 `30b`）。
  IMPROVEMENTS が「合計行に 3 つ目のチップが増えると窮屈」と書いた懸念は、
  チップを増やさないことで解消する。
- **計算式（`src/logic/profit.ts`）と `sale_records` の列は一切変更しない。**
  追加するのは新テーブル 2 本（`tags` / `record_tags`）だけ。既存の列は増やさない
  ── タグは 1 記録に複数付くので、列では表せない。
- **タグは付けなくても保存できる。** 必須にしない。入力の手間が増えて続かなくなるのが
  IMPROVEMENTS が挙げていた唯一の懸念で、必須化はその懸念をそのまま実現してしまう。

### 0.1 プリセットとタグの対比（形が違う理由）

| | プリセット（SPEC-V3） | タグ（本書） |
|---|---|---|
| 目的 | 入力を減らす | 記録を分類して後から絞り込む |
| 記録との関係 | **紐付かない**（値が欄に入るだけ） | **紐付く**（多対多） |
| 見た目 | 色 ＋ 1〜2 文字の頭文字バッジ | **色の点 ＋ 名前**のチップ |
| 数値 | 持つ（% / 円） | 持たない |
| 削除 | 物理削除で完結 | 中間テーブルの後始末が要る（§1.4） |
| 初期値 | 17 件を seed（§SPEC-V3 §2） | **seed しない**（§1.2） |

**見た目を分ける理由**（設計で確定済み）: プリセットは金額の行と 1 対 1 で位置が決まっているので
2 文字のバッジで足りる（どの行にあるかで意味が読める）。タグは**名前そのものを読む**必要があり、
長さも可変なので、色は識別の補助（点）に落とし、名前を本体にする。
**この違いは部品を分ける根拠でもある**（`PresetBadge` を流用せず `TagChip` を新設する。§2.3）。

---

## 1. データモデル

### 1.1 なぜ `presets` に同居させないか

| 案 | 内容 | 判断 |
|---|---|---|
| **A（採用）** | `tags` を独立したテーブルにする | **参照される側だから。** 中間テーブルの外部キーが `presets.id` を指すと、「参照されない行（プリセット 3 種）」と「参照される行（タグ）」が 1 つのテーブルに混ざり、削除の作法が `type` によって変わる。SPEC-V3 §1.1 が「消えても壊れる先がない」と書けたのは 3 種すべてがそうだったから |
| B（不採用） | `presets` に `type = 'tag'` を足す | 上記に加えて、`value`（数値）と `initial`（頭文字）が常に無意味な行が増える。編集画面も選択シートも「タグのときだけ隠す」分岐だらけになる。SPEC-V3 §0 が 3 種を 1 テーブルにしたのは**構造が同じだったから**で、構造が違うものを足すのは同じ原則の否定になる |

**色パレットは共有する。** `PresetColorKey`（8 色）と `theme.ts` の `presetTones` を
そのまま使う（§2.3）。名前に "preset" が入るが、実体は「明暗どちらでも読める 8 色の見本帳」なので
**改名しない**（参照箇所が広く、意味も変わらないため）。

### 1.2 型

```ts
export type Tag = {
  id: string;
  /** 自由入力。前後の空白を落として 1〜12 文字。「・」を含められない（§1.3） */
  name: string;
  /** 色パレットのキー（SPEC-V3 §1.3 の PresetColorKey をそのまま使う） */
  colorKey: PresetColorKey;
  /** 一覧・選択シート・チップの並び順（§1.5） */
  sortOrder: number;
};
```

| 項目 | 決定 | 理由 |
|---|---|---|
| `initial`（頭文字） | **持たない** | 名前をそのまま出すので導出する対象がない（§0.1） |
| `value`（数値） | **持たない** | タグは金額に一切関与しない |
| 初期値（seed） | **投入しない。0 件から始める**（決定 §9-7） | タグの語は扱う商材で決まる（洋服 / 食器 / 本 / 部品…）。汎用の初期値を置いても「消す手間」しか生まない。プリセットが 17 件を seed したのは「入力の型」を示す必要があったからで（SPEC-V3 §2.5）、タグは**入力欄の中で作れる**（§3.2）ので 0 件でも詰まらない |
| 色の割り当て | **追加時に「使用済みの色を避けて」自動で割り当てる。編集で変えられる**（決定 §9-8） | タグは数が増えやすく、作るたびに色を選ばせると新規作成が 2 段階になる。検索欄から作る経路（§3.2）では色を選ぶ画面自体が出せない |
| 登録件数の上限 | **なし**（決定 §9-11） | 選択シートに検索欄がある（§3.2）ので、増えても選べる |

**色の自動割り当ての規則**（`src/logic/tag.ts` の純粋関数。決定 §9-8）

```ts
/** 既存タグが使っていない色を、パレットの並び順で最初に見つけたもの。
 *  8 色すべてが使われていたら先頭から一巡する（重複を許す） */
export function nextTagColor(existing: Tag[]): PresetColorKey;
```

- 8 色を使い切った後に重複を許すのは、**名前が本体で色は補助**だから（§0.1）。
  色が被っても「◯洋服」「◯食器」は読み分けられる。使い切ったら色を増やす、という方向は採らない
  （明暗どちらでも読める色に限定したのが 8 色の理由。SPEC-V3 §1.3）。

### 1.3 検証規則

`src/logic/tag.ts`（新規・純粋関数）に置く。SPEC-V3 §1.4 と**2 点だけ違う**。

| 項目 | 規則 | 無効時の文言 |
|---|---|---|
| `name` | 前後の空白を落として **1〜12 文字**（書記素単位で数える。決定 §9-5） | 「名前を入れてください」 |
| `name` に使えない文字 | **「・」を含められない**（決定 §9-6） | 「「・」は使えません」 |
| 名前の重複 | **弾く**（前後空白を落として完全一致で比較） | 「同じ名前のタグがあります」 |
| `colorKey` | 読み出し時に `normalizePresetColor()` で正規化（SPEC-V3 §1.6 と同じ） | — |

- **12 文字**にしたのは、チップが横に並ぶ（フォームのタグ行・絞り込みシート・解除バー）ため。
  プリセットの 20 文字は 1 行に 1 つしか出ないので長くてよかった。
- **この制限を「入力欄の打ち止め」で実装しない**（SPEC-V3 §1.2 の共通規則。2026-08-11 追記）。
  `maxLength` や `onChangeText` での切り詰めは、日本語入力の**変換前のひらがな**まで数えるので、
  上限の近くで漢字に変換できなくなる（React Native は変換中かどうかを JS に出さない。
  iOS の `markedTextRange` も Android の composing span もネイティブ内部で完結している）。
  上限を超えたら**保存を止めて上の文言を出す**だけにして、入力そのものは切らない ──
  変換途中で 12 文字を超えても、確定して縮めば有効に戻る。
  プリセットの頭文字（2 文字）だけは切り詰めが要るので、そちらは `onBlur`（確定後）で行う。
- **「・」を禁じるのは CSV のため**（§5.2）。1 セルに複数のタグを「洋服・春夏物」と並べるので、
  区切り文字が名前に入ると往復で意味が壊れる。名前の側で禁じるのが最も確実で、
  書き出し時にエスケープを考えるより説明も短い。
- **重複を弾くのは、プリセットと逆の判断**（SPEC-V3 §1.4 は許可した）。
  プリセットは「見分けは色と頭文字が担う」ので同名でも困らなかったが、
  タグは**絞り込みの意味そのもの**なので、同名が 2 つあると
  「『洋服』で絞り込み中」と出したときにどちらのことか言えなくなる（§4.5 の解除バー）。

### 1.4 削除したときの中間テーブルの扱い

| 操作 | 中間テーブル | 決定 |
|---|---|---|
| タグを削除 | そのタグの行をすべて削除 | **repository が明示的に 2 文を実行する**（トランザクション内） |
| 記録を削除 | その記録の行をすべて削除 | 同上（`repository.remove` に 1 文足す） |
| 記録のタグを付け替え | その記録の行を**全消し → 入れ直し** | 差分計算をしない。1 記録あたり数件なので、消して入れ直す方が経路が 1 本で済む |

- **`ON DELETE CASCADE` に頼らない。** SQLite の外部キー制約は `PRAGMA foreign_keys = ON` が
  接続ごとに必要で、既定は OFF。expo-sqlite / better-sqlite3 のどちらでも
  「有効になっているつもり」が最も危ない（孤児行は絞り込みの件数を静かに狂わせる）。
  スキーマには `references()` を**意図の表明として書く**が、**削除の保証は repository 側で持つ**。
- **タグ削除は取り消せる**（設定画面の `UndoBar`。SPEC-V3 §3.2 と同じ扱い）。
  取り消しでは**タグ本体と、消した中間行の `recordId` の一覧の両方を書き戻す**
  ── 本体だけ戻すと、付いていた記録から静かに剥がれたままになる。
  よって `remove` は消す前の `recordId[]` を返す。

```ts
/** タグ本体と、そのタグが付いていた記録の id を返す（Undo が両方を書き戻すため。§1.4） */
remove(id: string): { tag: Tag; recordIds: string[] };
restore(tag: Tag, recordIds: string[]): void;
```

- **孤児行の掃除は持たない。** 上記 3 経路がすべてトランザクションで閉じているので、
  起動時のクリーンアップは要らない（あると「なぜ要るのか」の説明が要る）。

### 1.5 並び順

- `sortOrder` の昇順で固定。追加は末尾（`max(sortOrder) + 1`）。
  手動並べ替え・使用頻度順は実装しない（SPEC-V3 §3.4 / SPEC-V2 §7-9 を踏襲）。
- **記録に付いたタグの表示順も `tags.sortOrder` に従う。**
  中間テーブルは順序を持たない ── 付けた順を保つと、同じ 2 つのタグが記録ごとに違う並びで出る。

### 1.6 schema への影響

`src/db/schema.ts` に 2 テーブルを追加する。**既存のテーブル・列は一切変更しない。**

```ts
export const tags = sqliteTable('tags', {
  id: text('id').primaryKey(),                       // UUID（seed がないので固定 ID は不要）
  name: text('name').notNull(),
  colorKey: text('color_key').notNull(),             // PresetColorKey（§1.1 で共有）
  sortOrder: integer('sort_order').notNull().default(0),
}, (table) => [
  index('idx_tags_order').on(table.sortOrder),
]);

export const recordTags = sqliteTable('record_tags', {
  recordId: text('record_id').notNull().references(() => saleRecords.id),
  tagId: text('tag_id').notNull().references(() => tags.id),
}, (table) => [
  primaryKey({ columns: [table.recordId, table.tagId] }),   // 同じ組み合わせは 1 行だけ
  index('idx_record_tags_tag').on(table.tagId),             // タグ → 記録（絞り込み・使用件数）
]);
```

| 項目 | 決定 | 理由 |
|---|---|---|
| 中間テーブルの主キー | **複合主キー `(record_id, tag_id)`** | 代理キー（`id`）を持たせても使い道がない。複合 PK なら「同じタグを二重に付ける」が DB の側で起きなくなり、`record_id` 先頭のインデックスも自動で手に入る（記録 → タグの引き方） |
| `idx_record_tags_tag` | **足す** | 絞り込み（§4）と使用件数（§3.3）は「タグ → 記録」の向きに引く。複合 PK は `record_id` が先頭なのでこの向きには効かない |
| 追加の列（付けた日時など） | **持たない** | 使う画面がない。中間テーブルは関係だけを持つ |
| 保存先 DB | 既存の `profit-calculator.db` | プリセットと同じ判断（SPEC-V3 §1.6） |

**マイグレーション**は `drizzle/0003_*.sql` を生成する。**手での追記は不要**（seed がないため。
SPEC-V3 §1.6 が `INSERT` を手で足したのとはここが違う）。既存行への影響もない
（`sale_records` を触らないので、バックフィルという概念自体がない）。

---

## 2. タグの管理画面（設定タブ）

### 2.1 どの群に置くか

**決定: 「入力を減らす」群には入れない。新しい群「記録を分類する」を作り、その下に 1 枚置く。**

| # | 群 | 項目 | 本書での変更 |
|---|---|---|---|
| 1 | （見出しなし） | 使いかた ▸ | — |
| 2 | 記録 | 新規作成時の種別 | — |
| 3 | 入力を減らす | 販売サイト / 送料 / 梱包材（`PresetSummaryCard` 3 枚） | — |
| 4 | **記録を分類する** | **タグ ▸**（カード 1 枚。登録件数と、色の点のプレビュー） | **新設** |
| 5 | データ | 書き出し（CSV）/ 記録の件数 | — |
| 6 | フッタ | バージョン表記 | — |

- **群を分けるのは目的が違うから**（§0.1）。「入力を減らす」の 3 つは
  *選ぶと欄に値が入る*もので、タグは*記録に残って後から効く*もの。
  同じ群に 4 枚目として並べると、注記（「よく使う値を登録しておくと、記録するときに選ぶだけで入ります。」）が
  タグには当てはまらなくなる。
- 群 4 の注記 1 行: 「記録にタグを付けておくと、あとから『洋服だけ』のように絞り込めます。」
- 群 3 と 群 5 の間に置くのは、**設定を「入力 → 記録 → 出力」の順に読ませる**ため。

### 2.2 一覧画面

`app/(tabs)/settings/tags.tsx` → `src/screens/TagListScreen.tsx`（新規）。

**`PresetListScreen` は流用しない。** 理由は §1.1 と同じで、
あちらは `type` パラメータ・値の列（「210円」/「10%」）・頭文字を前提に組まれている。
`type` を 4 つ目に増やすと、値の列を出すかどうかの分岐が一覧・編集・プレビューの全部に入る。
**部品（`SwipeToDeleteRow` 相当・`UndoBar`・色の丸の横並び）は流用する。**

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | 「◀ 設定」／タイトル「タグ」 |
| 2 | 一覧カード | 1 行 = **色の点 ＋ 名前 ＋ 右端に使用件数（「12 件」）**。行タップで編集シート |
| 3 | 追加行 | カード末尾に「＋ 追加」 |
| 4 | 空表示 | 「タグがありません」＋「記録を追加するときにも作れます」＋「＋ 追加」 |
| 5 | 注記 | 「タグを消しても、記録そのものは消えません。」 |

- **削除は左スワイプ →「削除」＋ `UndoBar`**（SPEC-V3 §3.2 と同じ形）。確認アラートは挟まない。
- ただし**使用件数が 1 件以上のタグを消すときだけ**、`UndoBar` の文言を
  「『洋服』を削除しました（12 件の記録から外れました）」にする ── 記録から剥がれたことが
  取り消しの猶予の間に読めないと、`UndoBar` が消えてから気付くことになる。
- **使用件数は状態（売れた / 出品中）を問わない全記録で数える。**
  タグは記録の属性であって状態の属性ではない。

### 2.3 編集シート・チップ部品

`src/screens/TagFormSheet.tsx`（新規）。追加・編集で同じシートを使う。

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | 左「キャンセル」／中央「タグを追加」or「タグを編集」／右「保存」 |
| 2 | プレビュー | チップ（色の点 ＋ 名前）を実物と同じ形で 1 つ |
| 3 | 名前 | `TextField`。キャプション「名前（必須）」 |
| 4 | 色 | 8 色の丸を 1 行に横並び。選択中は外周にリング（SPEC-V3 §3.3-6 と同じ部品） |

`src/components/TagChip.tsx`（新規）を作る。`PresetBadge` は流用しない（§0.1）。

| 状態 | 見た目 |
|---|---|
| 表示のみ（記録詳細・解除バー） | 色の点（6px の丸）＋ 名前。枠線なし |
| 選択中・外せる（フォームのタグ行・絞り込みシート） | 上記 ＋ 薄い地 ＋ 右端に「✕」 |
| 未選択（絞り込みシートの候補） | 上記の地なし・枠線あり |

- **色の点は名前の左**。金額の行に出ることはないので、SPEC-V3 §1.3 が
  「バッジは手数料のオレンジ等と隣接しない」と気にした問題はここでも起きない。

---

## 3. 入力（案 `30a`）

### 3.1 記録フォームのタグ行

記録フォーム（`RecordFormSheet`）の伝票カードで、**商品名のすぐ下**に「タグ」の行を置く。

```
[ 商品名                            ]
[ タグ  ◯洋服 ✕  ◯春夏物 ✕   ＋   ]
[ 種別セレクタ                       ]
[ 販売価格 … 以下、金額の積み上げ      ]
```

| 項目 | 決定 | 理由 |
|---|---|---|
| 置き場所 | **商品名の直下・種別セレクタの上**（決定 §9-3） | タグは「何を売ったか」の情報で、金額ではない。金額の積み上げの中に混ぜると伝票の読み（販売価格 − 内訳 = 結果）が途切れる。種別は「金額の積み上げの直前」（UI-SPEC §6-6）を保ったまま 1 つ下がる |
| 0 件のとき | 「タグ」ラベル ＋「＋」だけの行を出す（行ごと消さない） | 出したり消したりすると機能に気付けない（SPEC-V3 §4.1 と同じ判断） |
| チップの並び | `tags.sortOrder` 昇順（§1.5） | |
| 行の折り返し | チップが増えたら**折り返して 2 段目に伸びる** | 横スクロールにすると端のタグが見えない。数が増えるのは利用者が選んだ結果なので、高さが伸びるのは受け入れる |
| 「✕」 | チップの右端。押すとその場で外れる | シートを開き直さずに 1 つ外せる |

**この配置は UI-SPEC §6-6「種別セレクタは商品名の直下」と衝突していた**（§8-3）。
**決定 §9-3 に従い、UI-SPEC §6-6 の側を「タグ行の直下」に改訂済み。**

### 3.2 タグ選択シート（複数選択）

「＋」を押すと下から出る。

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | 左は空／中央「タグ」／右「完了」（太字・青。0 件でも押せる） |
| 2 | 検索欄 | 「タグを探す・作る」。入力で一覧を絞り込む |
| 3 | 新規作成行 | 検索語に**完全一致する既存タグがないとき**だけ先頭に出す:「＋『洋服』を作る」 |
| 4 | 一覧 | 1 行 = **チェックボックス ＋ 色の点 ＋ 名前 ＋ 右端に使用件数** |
| 5 | 末尾 | 「設定で編集する ▸」 |

- **「完了」で閉じる**（`PresetMultiPickerSheet` の「入れる」と違い、**選択は即座にフォームの state へ反映**して
  よい ── 積み上げではなく集合の編集なので、確定の瞬間を作る意味がない）。
  ただし**DB への書き込みはフォームの「保存」まで行わない**（UI-SPEC §8.6 / SPEC-V3 §3.3 を踏襲）。
- **新規作成はその場で `tags` に書き込む**（フォームを保存しなくても残る）。
  ここだけ書き込みが先行するのは、タグ本体が「フォームの下書き」ではなく共有の資産だから。
  色は自動割り当て（§1.2）。作った直後にチェック済みになる。
- 検索は前方一致ではなく**部分一致**（既存の `LIKE` と同じ感覚）。JS 側で絞る（数十件規模）。
- **使用件数を右端に出すのは、選ぶときに「どれが生きているタグか」が分かるようにするため。**
  同じ数字を設定画面（§2.2）とここの両方に出すので、数え方（状態を問わない全記録）を揃える。

### 3.3 使用件数の引き方

**1 本のクエリで全タグぶんまとめて数える**（タグごとに 1 本引くと N+1 になる）。

```ts
/** tagId -> 件数。0 件のタグはキーごと現れないので、呼び出し側で ?? 0 する */
countsByTag(): Map<string, number>;
```

```sql
SELECT tag_id, count(*) FROM record_tags GROUP BY tag_id;   -- idx_record_tags_tag が効く
```

### 3.4 計算タブとレコード詳細

| 画面 | 扱い |
|---|---|
| 計算タブ | **タグ行を置かない**（決定 §9-4）。計算タブは金額を試算する面で、記録の分類はまだ決まっていない。`siteName` のように state で引き継ぐこともしない |
| レコード詳細 | **メタ行の下・商品名の直後にチップを表示のみ**（「✕」なし。決定 §9-12）。**レシートカードの外側**に置く ── レシートカードは「販売価格 − 内訳 = 結果」の金額の面（UI-SPEC §1.4）で、タグは金額ではないため中に入れない。編集はフォーム経由（`SiteNameRow` と同じ扱い。SPEC-V3 §1.5.1） |
| 記録一覧の行 | **出さない。** 行の情報量を増やさない（UI-SPEC §6-1 / §6-2 の方針。SPEC-V3 §1.5.1 も同じ判断） |

---

## 4. 絞り込み（案 `30b`）

### 4.1 合計行の作り替え

**現状**（UI-SPEC §1.2-3 / `RecordListScreen`）: 左に集計 2 値、右端に**状態チップ**と**種別チップ**の 2 つ。
**本書**: 状態は**セグメント（横幅いっぱい）**に、種別は**シートの中**へ移す。

| 要素 | 変更 |
|---|---|
| 状態チップ（「売れた記録 ▾」⇄「出品中 ▾」のトグル） | **廃止 → セグメント**「売れた記録 / 出品中」。押した先が見えるようになる |
| 種別チップ（すべて → 不用品 → 仕入品 の巡回） | **廃止 → 絞り込みシートの中のセグメント**（§4.2） |
| 新規 | **「絞り込み N」チップ**。N = 効いている絞り込みの数。0 のときは「絞り込み」だけ |

**段の割り付け（決定 §9-1）**

```
[ ヘッダ                       記録   ⌕ ⇅ ？ ]   1 段目
[ 月バー            ◀  2026年8月 ▾  ▶       ]   2 段目
[ この月の収支 ¥12,300      経費 ¥4,100      ]   3 段目（集計 2 値）
[ ( 売れた記録 | 出品中 )        絞り込み 2   ]   4 段目（セグメント ＋ チップ）
[ 仕入品・タグ「洋服」で絞り込み中   |   解除  ]   5 段目（絞り込み中のみ。案 21d）
```

- **集計 2 値を上、セグメントを下**にするのは、**集計は常に見えているべきで、
  セグメントは操作だから**。操作を値の上に置くと、目が最初に当たるのが数字ではなくボタンになる。
- **UI-SPEC §1.2 の「上部の固定段を 3 段に収める」は本書で改訂する**（§8-2 / 更新済み）。
  通常 4 段、絞り込み中は 5 段。段が増えるぶんリストの見える件数は減るが、
  **絞り込みが効いている事実を隠すより高さを使う方を採る**（案 `21d` の趣旨）。
- 5 段目は絞り込みが 0 件のときは**行ごと出ない**ので、通常の高さは 4 段で固定。

**N の数え方（決定 §9-2）**

- **条件の本数を数える**（種別 1 ＋ 販売サイト 1 ＋ タグ 1 = 最大 3）。
  タグを 3 つ選んでも OR なので**条件としては 1 本**。
- 巡回チップをやめるのは、選択肢が 3 つ（種別）で済んでいたから成立していた形で、
  販売サイトとタグが加わると巡回では表現できないため。
- **期間（月バー）と検索（ヘッダの ⌕）は N に含めない。** どちらもシートの外に
  専用の UI があり、効いていることが画面から直接読める。
- 解除バー（§4.3）に並ぶ条件の数と N は**必ず一致する**（同じ関数から出す）。

### 4.2 絞り込みシート

| # | 要素 | 内容 |
|---|---|---|
| 1 | ヘッダ | 左「すべて解除」／中央「絞り込み」／右「完了」 |
| 2 | 種別 | セグメント「すべて / 不用品 / 仕入品」（`KIND_FILTER_OPTIONS` をそのまま使う） |
| 3 | 販売サイト | **単一選択**。現在値を右に出し、押すと一覧へ（push か 2 枚目のシート）。**件数の上限も検索欄も持たない**（決定 §9-10） |
| 4 | タグ | **複数選択のチップ**。2 つ以上選ぶと **OR** |
| 5 | 下部 | 「この条件に合う記録 N 件」 |

- **「完了」で閉じるが、条件は選んだ瞬間から効く**（下部の N 件がその場で動くため）。
  「すべて解除」は種別・販売サイト・タグの 3 つだけを初期値に戻す（**期間・検索・並び替えは動かさない**）。
- **販売サイトの候補は `presets(type='site')` ではなく、記録に実在する `site_name` の集合**から作る。
  プリセットを消しても、その名前で保存された記録は残っているため（SPEC-V3 §1.5.1 の「写し」の帰結）。

```sql
SELECT DISTINCT site_name FROM sale_records WHERE site_name <> '' ORDER BY site_name;
```

  ここは**候補の一覧を作るクエリなので `DISTINCT` が正しい**（§4.4 で `DISTINCT` を退けるのは
  レコードを引くクエリの話で、別物）。
- **上限も検索欄も置かずに始める**（決定 §9-10）。候補として並ぶのは
  「実際に使ったサイトの数」だけで、プリセットを何件登録しても増えない。
  実際に多くなってから対応する（`docs/IMPROVEMENTS.md` に記録する）。

**出品中を選んだときの挙動**

| 状態 | シート |
|---|---|
| 売れた記録 | 種別 / 販売サイト / タグ の 3 節 |
| 出品中 | **販売サイトの節が消える**（種別 / タグ の 2 節） |
| 出品中 → 売れた記録 に戻す | **直前の販売サイトの指定を復元する** |

- 出品中の記録は `site_name` が空（まだ売れていないのでサイトが決まっていない。
  IMPROVEMENTS「販売サイトでの絞り込み」が挙げていた懸念そのもの）。
  節を残すと「選ぶと必ず 0 件になる欄」になる。
- 復元は**画面がマウントされている間だけ**保つ（`lastSiteName` を state に持つ）。
  タブを離れて戻れば消える（決定 §9-9: 絞り込みは画面ローカルの state のまま。永続化しない）。
- **状態を切り替えても効いた条件は、SQL の側でも無視する**（画面で節を消すのと二重にする）。
  `isSoldMode === false` のときは `siteName` 条件を組み立てない ── 画面の見た目だけで
  条件を落とすと、復元した瞬間に「見えないのに効いている」状態を作り得る。

### 4.3 解除バー（案 `21d`）

絞り込みが 1 つでも効いているとき、**合計行の下に 1 段**出す。

```
仕入品・タグ「洋服」で絞り込み中                          | 解除
```

- 文言は純粋関数で組み立てる（画面で文字列を連結しない）:

```ts
/** 効いている条件を「仕入品・タグ「洋服」」のように連ねる。0 件なら null（バーごと出さない） */
export function filterSummaryText(filter: RecordFilter, tags: Tag[]): string | null;
```

- タグが 2 つ以上のときは「タグ「洋服」ほか 1 件」と畳む（全部並べると 1 行に収まらない）。
- 「解除」は「すべて解除」と同じ（種別・販売サイト・タグを初期値へ）。
- **N の数え方と文言の対象は一致させる**（§4.1 の N が 2 なら、バーにも 2 つ並ぶ）。

### 4.4 クエリ設計 — `EXISTS` を使い、`DISTINCT` は使わない

**決定: 複数タグの OR は相関サブクエリ（`EXISTS`）で書く。`JOIN` ＋ `DISTINCT` は採らない。**

```sql
-- buildWhere に足す 1 条件（tagIds が 1 件以上のときだけ）
EXISTS (
  SELECT 1 FROM record_tags
  WHERE record_tags.record_id = sale_records.id
    AND record_tags.tag_id IN (?, ?, ...)
)
```

| 案 | 内容 | 判断 |
|---|---|---|
| **A（採用）** | `EXISTS` の相関サブクエリ | **行が増えないので `DISTINCT` が要らない。** 既存の `buildWhere` に条件を 1 本足すだけで、`filteredRecords` / `careerSummary` / `earliestMonthKey` / `filteredAndGrouped` の**4 経路すべてに同時に効く**。`idx_record_tags_tag` が効く |
| B（不採用） | `JOIN record_tags` ＋ `SELECT DISTINCT` | **集計が壊れる。** `careerSummary` / `analyticsSummary` は `sum(netProfit)` を引いており、2 つのタグが付いた記録は 2 行に増える。`DISTINCT` は行の重複を消すだけで**集計の重複計上は消せない**（`sum(DISTINCT x)` は別の意味になる）。一覧だけ直しても合計行が静かに倍になる |
| C（不採用） | `id IN (SELECT record_id FROM record_tags WHERE tag_id IN (...))` | 動作は A と同じだが、相関がないぶん SQLite が中間結果を作りやすい。A の方が既存の書き方（`sql` テンプレート）にも素直に乗る |

**質問への回答**: 「複数タグの OR 検索には `DISTINCT` が必要になる」は **JOIN で書いた場合に限って正しく、
そしてその `DISTINCT` は集計クエリを直せない**。`EXISTS` にすれば `DISTINCT` は最初から不要になる。
これが本書で `EXISTS` を採る決定的な理由。

**AND 検索（選んだタグをすべて持つ）は V4 では持たない**（設計で OR 固定と確定済み）。
必要になったら `EXISTS` をタグの数だけ `AND` で並べるだけで足りる（構造を変えずに拡張できる）。

### 4.5 既存の `buildWhere` との関係

`RecordListFilter` に 2 つ足す。**型の追加だけで、既存の 3 条件（`isSold` / `monthKey` / `searchText` / `kind`）は
一切変更しない。**

```ts
export type RecordListFilter = {
  isSoldMode: boolean;
  searchText?: string;
  monthKey?: string | null;
  kind?: RecordKind | null;
  /** 販売サイト名の完全一致（§4.2）。null/undefined = すべて。isSoldMode=false のときは無視される */
  siteName?: string | null;
  /** タグの OR 条件（§4.4）。空配列・undefined = すべて */
  tagIds?: string[];
};
```

```ts
function buildWhere(filter: RecordListFilter): SQL {
  const conditions: SQL[] = [eq(saleRecords.isSold, filter.isSoldMode)];
  // …既存の monthKey / searchText / kind はそのまま…
  if (filter.isSoldMode && filter.siteName) {
    conditions.push(eq(saleRecords.siteName, filter.siteName));
  }
  if (filter.tagIds && filter.tagIds.length > 0) {
    conditions.push(sql`EXISTS (…)`);
  }
  return sql.join(conditions, sql` AND `);
}
```

**5 つの条件の組み合わせ**

| 条件 | 出所 | リスト本体 | 合計行（`summaryFilter`） |
|---|---|---|---|
| 状態（`isSold`） | 合計行のセグメント | ○ | ○ |
| 期間（`monthKey`） | 月バー・期間シート | ○ | ○ |
| 検索（`searchText`） | ヘッダの ⌕ | ○ | **×** |
| 種別（`kind`） | 絞り込みシート | ○ | ○ |
| **販売サイト（`siteName`）** | 絞り込みシート | ○ | **○** |
| **タグ（`tagIds`）** | 絞り込みシート | ○ | **○** |

- 検索だけが合計行に効かないのは既存の決定（「検索は探す操作で、見る対象そのものの限定ではない」。
  `RecordListScreen` のコメント / SPEC-V2 §4.2）。**販売サイトとタグは種別と同じ「限定」なので合計行にも効く。**
  これは「タグ『洋服』で絞ったときの収支合計」を見たいという機能の目的そのもの。
- **期間シートの月グリッド（`monthsWithRecords`）は絞り込みを一切受け取らない方針を維持する**
  （UI-SPEC §1.2 の派生決定）。タグでも変えない ── 絞り込むたびにグリッドの濃淡が変わると
  期間選びの手がかりとして当てにならなくなる、という理由はタグでもそのまま成り立つ。

### 4.6 シート下部の「この条件に合う記録 N 件」

repository に件数だけを返す 1 本を足す。

```ts
/** buildWhere と同じ条件での件数（§4.2 の下部表示）。集計はしない */
countRecords(filter: RecordListFilter): number;
```

- シートは**下書きの条件**を持ち、変わるたびにこれを引く。同期クエリで数千件規模
  （SPEC-V2 §8-6）なので、タップごとに引いて問題にならない。
- 検索語は含めない（合計行と同じ扱い。シートに検索欄がないので混乱もない）。

### 4.7 タグが消えたときの絞り込み

別画面（設定タブ）でタグを消すと、`tagIds` に存在しない id が残り得る。

- **画面復帰時（`useFocusEffect`）に、生きているタグの id へ絞り直す。**
  `EXISTS` は存在しない id を単に無視するので SQL は壊れないが、
  **解除バーの文言と N の数が実体と合わなくなる**（消えたタグの名前が引けない）ので、state 側で落とす。
- すべて落ちて 0 件になったら、その条件は解除された扱いになる（バーも消える）。

### 4.8 0 件のときの空表示

**決定（§9-13）: 文言を 1 つに統一し、条件の名前は出さない。**

| # | 要素 | 内容 |
|---|---|---|
| 1 | 見出し | 「条件に合う記録がありません」 |
| 2 | リンク | 「絞り込みを解除」（種別・販売サイト・タグを初期値へ。「すべて解除」と同じ） |

- **条件ごとの文言を作らない。** 効き得る条件は 6 つ（状態・期間・検索・種別・販売サイト・タグ）で、
  組み合わせごとに文言を書くと爆発する。SPEC-V2 の段階では検索と種別の 2 つしかなかったので
  出し分けられていたが、本書でその前提が崩れる。
- **既存の `ListEmpty` の 2 通りの出し分け（検索中 / 種別で絞り込み中）も、この 1 文言に統合する。**
  「ほかの月にはあるかもしれません」のような推測の文も落とす ── 期間も条件の 1 つになったので、
  特定の条件だけを名指しして示唆する根拠がなくなる。
- **1 件も記録がないとき**（絞り込みが 0 件で、かつ記録が 0 件）は従来どおり別の空表示を出す
  （「まだ記録がありません」＋ 追加への導線）。**絞り込みの有無で 2 通り**に整理する。
- 検索中の 0 件もこの文言にする。検索欄はヘッダに出たままなので、
  何で絞れているかは画面から読める。

---

## 5. CSV への影響

### 5.1 前提 — CSV はまだ実装されていない

SPEC-V3 §5 の CSV は **Step 5・6 が未着手**（設定タブの行は「準備中」のまま）。
したがって本書は **SPEC-V3 §5.3 の 17 列の表を 18 列に改訂する**。
実装がまだなので「後から列を足す」のではなく、**最初から 18 列で作る**。

### 5.2 決定 — 1 列に「・」で並べる

**決定: タグは 1 列。1 件の記録に付いた複数のタグを「洋服・春夏物」と 1 セルに並べる。**

| 案 | 内容 | 判断 |
|---|---|---|
| **A（採用）** | 1 列に区切り文字で並べる | **1 行 = 1 記録**が保てる。列数がデータの中身で変わらない。表計算では `COUNTIF(範囲,"*洋服*")` で扱える |
| B（不採用） | タグ 1 / タグ 2 / タグ 3 … と列を展開 | **列数がその時の最大タグ数で変わる**。ヘッダが書き出しごとに変わると、取込側の設定を毎回やり直すことになる。SPEC-V3 §5.2 が「出力は 1 種」に統一した趣旨にも反する |
| C（不採用） | タグごとに行を複製（1 行 = 1 記録 × 1 タグ） | **金額が重複計上される。** 2 つタグが付いた記録は販売価格も収支も 2 回足される。SPEC-V3 §5.4 が「CSV の中で足し算が合う方を採る」と決めた原則を真っ向から壊す |
| D（不採用） | タグごとに 0/1 の列を立てる（ダミー変数） | 列数がタグの登録数で変わる（B と同じ問題がより強く出る） |

**区切り文字は「・」1 文字。タグ名に「・」を使えなくする**（§1.3 の検証で禁じる）。

- エスケープを設計するより、**入力の側で 1 文字を予約する方が説明も実装も短い**。
  設定画面で「・」を打った瞬間に理由が出る（「「・」は使えません」）ので、
  書き出してから気付く経路にならない。
- 「,」を使わないのは CSV の区切りと衝突するため（引用で回避できるが、
  表計算の「区切り位置」でセル内を分割したいときに使えない）。
- **並び順は `tags.sortOrder` 昇順**（画面の表示と同じ。§1.5）。
  書き出すたびに並びが変わらないので、差分を取れる。
- タグなしの記録は**空文字**（SPEC-V3 §5.4「空値は空文字」に従う）。

### 5.3 改訂後の列（18 列）

SPEC-V3 §5.3 の 17 列に**14 列目として「タグ」を挿入**する。以降の列は 1 つずつ後ろへずれる。

| # | 列名 | 出所 | 備考 |
|---|---|---|---|
| 1–12 | （SPEC-V3 §5.3 のまま） | | **先頭 3 列の固定は変わらない**（販売日 / 商品名 / 販売価格） |
| 13 | 種別 | `kind` | |
| **14** | **タグ** | `record_tags` → `tags.name` | **新設。「洋服・春夏物」。なしは空** |
| 15 | 状態 | `isSold` | 旧 14 |
| 16 | 出品日 | `saleStartDate` | 旧 15 |
| 17 | メモ | `memo` | 旧 16 |
| 18 | 記録ID | `id` | 旧 17。**末尾のまま** |

- **12（販売サイト）と 13（種別）の隣に置く**のは、この 3 つが「属性の群」だから
  （SPEC-V3 §5.2 の並び「内訳 → 計算値 → 属性 → メモ → 記録ID」に従う）。
- 「メモ」より前に置くのは、メモが改行を含み得る（引用が必要な）列で、
  その後ろに読みたい列を置くと表計算で目視しづらいため。

### 5.4 書き出しクエリへの影響

- `listForExport()`（SPEC-V3 §6.1）は `SaleRecord[]` を返す設計だった。
  **タグは 1 対多なので同じ行に載らない。** よって書き出し用に 1 本足す:

```ts
/** 対象の記録に付いたタグを 1 本のクエリでまとめて引く（記録ごとに引かない） */
tagNamesByRecord(recordIds: string[]): Map<string, string[]>;
```

- `csv.ts` は**純粋関数のまま**にする（SPEC-V3 §6.1 の方針）。
  引数を `(records, tagsByRecord)` の 2 つにして、DB は呼び出し側が引く。
- **書き出しの絞り込みにタグは使わない**（決定 §9-14）。`ExportSheet` が持つのは期間と対象だけ
  （SPEC-V3 §5.5）。CSV の主用途はバックアップ（SPEC-V3 §0.1）で、
  絞り込んだ書き出しは「一部しか保存されていないファイル」を作る事故の方が大きい。
  タグごとの集計は書き出した後に表計算側で行う（§5.2 の `COUNTIF`）。

---

## 6. データタブへの影響

**決定: 適用する。** ただし記録タブと同じ形にするのではなく、**同じシートを状態の節なしで使う**。

| 項目 | 記録タブ | データタブ |
|---|---|---|
| 状態 | セグメント（売れた記録 / 出品中） | **なし**（`isSold = true` 固定。SPEC §6.2） |
| 期間 | 月バー | 月バー（共用） |
| 絞り込みシート | 種別 / 販売サイト / タグ | **同じ 3 節**（販売サイトの節は常に出る） |
| 現状の種別チップ（巡回） | 廃止 | **廃止**（記録タブと揃える） |
| 絞り込みの state | 画面ローカル | **画面ローカル。記録タブとは共有しない**（決定 §9-9） |

- データタブは元から売れた記録だけを見る面なので、**販売サイトの節を消す分岐が要らない**
  （§4.2 の分岐は記録タブ側だけの話）。
- `AnalyticsFilter` に `siteName` / `tagIds` を足し、`buildAnalyticsWhere` に
  `buildWhere` と**同じ `EXISTS` の式**を足す。式は共通の関数に切り出して 1 か所で持つ:

```ts
/** タグの OR 条件（§4.4）。buildWhere / buildAnalyticsWhere の両方が使う */
function tagExistsSql(tagIds: string[]): SQL;
```

- **チャート（`aggregatedPoints`）にも同じ条件が効く**。集計式（`netProfitSql` 等）は変更しない ──
  `EXISTS` は行を増やさないので、SUM はそのまま正しい（§4.4 の A を採った帰結）。
- これで IMPROVEMENTS が挙げていた「『洋服だけの売上』が見えると仕入れの判断材料になる」が
  データタブのグラフとして満たされる。**タグ機能の主目的はここにある**ので、
  データタブへの適用は「余力があれば」ではなく**必須の範囲**として扱う。

---

## 7. 実装の影響範囲とステップ分割

### 7.1 触るファイル

| ファイル | 変更内容 | Step |
|---|---|---|
| `src/db/schema.ts` | `tags` / `record_tags` を追加（§1.6）。既存テーブルは変更しない | 1 |
| `drizzle/0003_*.sql`（生成のみ） | 2 テーブル ＋ 2 インデックス。**手での追記なし** | 1 |
| `src/db/tags.ts`（新規） | `listAll` / `create` / `update` / `remove` / `restore` / `countsByTag` / `siteNames`（§1.4 / §3.3 / §4.2） | 1 |
| `src/db/recordTags.ts`（`tags.ts` に同居可） | `setTagsForRecord` / `tagIdsByRecord` / `tagNamesByRecord`（§1.4 / §5.4） | 1 |
| `src/db/repository.ts` | `remove` で中間行も消す（§1.4）／`SaveRecordInput.tagIds` を追加（**必須**。フィクスチャも要修正）／`create`・`update` をトランザクション化／`buildWhere`・`buildAnalyticsWhere` に 2 条件（§4.5 / §6）／`countRecords` を追加（§4.6）。**計算式の SQL は変更しない** | 1・4 |
| `src/logic/tag.ts`（新規） | 検証（§1.3）・色の自動割り当て・チップの並び。純粋関数 ＋ 単体テスト | 1 |
| `src/db/useTags.ts`（新規） | 画面用フック（`usePresets.ts` と同じ形） | 1 |
| `src/components/TagChip.tsx`（新規） | 色の点 ＋ 名前。3 つの見た目（§2.3） | 2 |
| `src/screens/TagListScreen.tsx`（新規） | 一覧・追加・スワイプ削除 ＋ `UndoBar`（§2.2） | 2 |
| `src/screens/TagFormSheet.tsx`（新規） | 追加・編集シート（§2.3） | 2 |
| `app/(tabs)/settings/tags.tsx`（新規） | ルート | 2 |
| `app/(tabs)/settings/index.tsx` | 群「記録を分類する」を新設（§2.1） | 2 |
| `src/components/TagPickerSheet.tsx`（新規） | 複数選択 ＋ 検索欄からの新規作成（§3.2） | 3 |
| `src/logic/recordForm.ts` / `.test.ts` | `RecordFormValues.tagIds` ／ `newFormValues`（空配列）／ `toSaveInput`（§3.1） | 3 |
| `src/screens/RecordFormSheet.tsx` | 商品名の下にタグ行（§3.1） | 3 |
| `src/components/RecordDetailSections.tsx` | 商品名の下にチップを表示のみ（§3.4） | 3 |
| `src/logic/recordFilter.ts`（新規） | 絞り込みの下書き型・`activeFilterCount`・`filterSummaryText`・`clearAll`（§4.1 / §4.3）。`kindFilter.ts` を内側で使う | 4 |
| `src/components/FilterSheet.tsx`（新規） | 絞り込みシート（§4.2）。記録タブ・データタブで共用（`showSite` を切る） | 4・5 |
| `src/components/FilterClearBar.tsx`（新規） | 解除バー（案 `21d`。§4.3） | 4 |
| `src/components/SummaryBar.tsx` | **2 段構成にする**（1 段目 = 集計 2 値／2 段目 = セグメント ＋ チップ。§4.1 / 決定 §9-1）。`trailing` にチップを並べる現在の形は廃止 | 4 |
| `src/screens/RecordListScreen.tsx` | 合計行の作り替え（§4.1）／`cycleKindFilter` を廃止／解除バー／`useFocusEffect` で消えたタグを落とす（§4.7） | 4 |
| `src/screens/RecordListScreen.tsx` の `ListEmpty` | **空表示を 2 通りに統合**（絞り込みあり / 記録なし。§4.8 / 決定 §9-13）。条件ごとの出し分けを削る | 4 |
| `src/components/OptionSheet.tsx` の呼び出し（並び替えシート） | 先頭の「絞り込みをすべて解除」を**外す**（§8-6） | 4 |
| `src/screens/DataScreen.tsx` | 種別チップを廃止し「絞り込み N」＋シートへ（§6） | 5 |
| `src/db/useRecords.ts` | フィルタ型の追加に追随（クエリは repository のまま） | 4・5 |
| `src/logic/csv.ts`（SPEC-V3 Step 5 で新規） | **18 列で作る**（§5.3）。タグ列の組み立て | 6 |
| `src/logic/labels.ts` | 群の見出し・シートの見出し・解除バーの文言・CSV の列名・検証の文言 | 各 Step |
| `src/screens/HelpScreen.tsx` | 1 節追加（§7.4） | 7 |
| ~~`docs/UI-SPEC.md`~~ | **2026-08-11 に反映済み**（§1.2 の合計行・チップ・段数、§1.4 の詳細画面、§6-6 の並び、並び替えシート）。いずれも「SPEC-V4 で改訂（未実装）」と明記してある | — |
| ~~`docs/SPEC-V3.md`~~ | **2026-08-11 に反映済み**（§1.4 の重複規則の対比、§1.5.1 の絞り込み、§5.3 の 18 列） | — |
| ~~`docs/IMPROVEMENTS.md`~~ | **2026-08-11 に反映済み**（「タグによる分類」「販売サイトでの絞り込み」を本書へ移した） | — |
| `docs/SPEC.md` | §1 にテーブル定義、設定タブの項目一覧を反映（**実装後**。SPEC.md は実装済みの姿を書く文書のため） | 7 |
| `docs/UI-SPEC.md` / `docs/SPEC-V3.md` / `docs/IMPROVEMENTS.md` | 実装完了時に「（未実装）」の断り書きを外す | 7 |

**変更しないファイル**: `src/logic/profit.ts`（計算式）、`src/logic/input.ts`、
`src/logic/analytics.ts`、`src/db/dates.ts`、`src/components/RecordRow.tsx`（§3.4）、
`src/components/PeriodSheet.tsx`（§4.5）、`src/components/PresetBadge.tsx`（§0.1）、
`src/logic/kindFilter.ts`（`recordFilter.ts` から使うだけ）。

### 7.2 ステップ

| Step | 内容 | 完了条件 |
|---|---|---|
| 1 | テーブル ＋ マイグレーション ＋ repository ＋ 純粋関数 ＋ 保存経路 | 既存の全テストが通る。タグを付けた記録を保存・取得できる。**UI の見た目は変わらない** |
| 2 | 設定タブの管理画面（一覧・追加・編集・削除・Undo）＋ `TagChip` | タグを自由に編集できる。使用件数が出る |
| 3 | 記録フォームのタグ行 ＋ 選択シート ＋ 詳細画面での表示（案 `30a`） | 記録にタグを付けて保存でき、詳細で読める。**まだ絞り込めない** |
| 4 | 記録タブの合計行の作り替え ＋ 絞り込みシート ＋ 解除バー（案 `30b` / `21d`） | 種別・販売サイト・タグで絞り込め、合計行がその条件で集計される |
| 5 | データタブへ適用 | グラフと合計行がタグで絞り込める（**機能の主目的。§6**） |
| 6 | CSV のタグ列 | SPEC-V3 Step 5 と合流。18 列で書き出せる |
| 7 | ヘルプ・各仕様書の更新 | ドキュメントが実装と一致している |

- **Step 1 → 2 → 3 は順に必須**（テーブルなしで UI を作らない。SPEC-V3 §6.2 / SPEC-V2 §6.2 と同じ理由）。
- **Step 4 は Step 3 に依存する**（タグの付いた記録が 1 件もないと、絞り込みを作っても確かめられない）。
- **Step 5 は Step 4 の後**（シートを共用するため）。Step 6 は Step 1 だけに依存し、
  SPEC-V3 Step 5 の実装時期に合わせて前後してよい。
- Step 3 で止めても壊れない（タグを付けられるが絞り込めない、という一貫した状態）。

### 7.3 テスト方針

- `src/logic/tag.test.ts`: 検証の境界（0 / 1 / 12 / 13 文字、「・」を含む名前、前後空白、
  重複の判定）、**色の自動割り当てが使用済みの色を避けること・8 色すべて使用済みなら先頭へ戻ること**（決定 §9-8）。
- `src/logic/recordFilter.test.ts`: `activeFilterCount`（0〜3）、`filterSummaryText`
  （0 件は null / 1 件 / タグ 2 つの畳み込み）、`clearAll` が期間・検索・並び替えを動かさないこと。
- `src/db/tags.test.ts`（新規）: CRUD、`sortOrder` の採番、**タグ削除で中間行が消えること**、
  **`restore` で中間行も戻ること**、**記録削除で中間行が消えること**、`countsByTag`。
- `src/db/repository.test.ts`（既存に追加）: **タグ 2 つが付いた記録が `careerSummary` で
  二重に計上されないこと**（§4.4 の B を退けた理由をテストで固定する）、
  OR の絞り込み、`isSoldMode=false` で `siteName` 条件が無視されること、`countRecords`。
- `src/logic/csv.test.ts`（SPEC-V3 Step 5 で新規）: タグ列が `sortOrder` 順に「・」で並ぶこと、
  タグなしが空文字になること、18 列であること。
- 画面のスナップショットは取らない（SPEC-V3 §6.3 と同じ）。

### 7.4 ヘルプに追加する節

| 節 | 明記する内容 |
|---|---|
| タグで分類する | タグの付け方（記録するときに商品名の下から）／タグは付けなくてもよい／**タグを消しても記録は消えない**（§1.4）／絞り込みは記録タブとデータタブの両方で効く／CSV には「・」区切りで 1 列に入る（§5.2） |

---

## 8. 既存の決定との関係（矛盾する箇所）

**6 件が実際に衝突し、2026-08-11 に §9 の決定へ沿って各仕様書を更新済み。**
更新した箇所には「**SPEC-V4 で改訂（未実装）**」と明記してあり、
実装完了時に断り書きを外す（§7.1 の末尾 2 行）。

1. **UI-SPEC §1.2-3「合計行: 左に集計 2 値、右端に状態チップと種別フィルタチップの 2 つ」**
   → **衝突。決定 §9-1 / §9-2 に沿って UI-SPEC §1.2 の当該行と「チップ」の節を差し替えた。**
   状態はセグメントへ、種別は絞り込みシートへ移り、右端のチップは「絞り込み N」1 つになる。
2. **UI-SPEC §1.2「上部の固定段を 3 段に収める（ヘッダ + 月バー + 合計行）」**
   → **衝突。決定 §9-1 に沿って「通常 4 段・絞り込み中は 5 段」に改訂した**（UI-SPEC §1.2 更新済み）。
   合計行が 2 段（集計 2 値 / セグメント ＋ チップ）になり、解除バーが 5 段目に入る。
3. **UI-SPEC §6-6「種別セレクタは商品名の直下・金額の積み上げの直前」**
   → **衝突。決定 §9-3 に沿って「タグ行の直下・金額の積み上げの直前」に改訂した**（UI-SPEC §6-6 更新済み）。
   並びは 商品名 → タグ → 種別 → 金額の積み上げ。
4. **SPEC-V3 §1.5.1「販売サイトでの絞り込みは V3 では行わない」／IMPROVEMENTS「販売サイトでの絞り込み」**
   → **矛盾ではなく、先送りの解消。** IMPROVEMENTS が挙げた 2 つの懸念は本書で解ける:
   「チップが 3 つで窮屈」→ チップを増やさずシートに入れた（§4.1）。
   「出品中はサイトを持たない」→ 出品中では節ごと消す（§4.2）。
   同項の「実装するなら絞り込み UI をまとめて設計する必要がある」がそのまま本書。
   **SPEC-V3 §1.5.1 の表と IMPROVEMENTS の当該項を更新済み。**
5. **SPEC-V3 §1.4「名前の重複は許可する（弾かない）」**
   → **タグでは逆にする**（§1.3）。同じ機能群に見えて扱いが違うので、
   **両方の文書に「なぜ違うか」を残した**（プリセットは色と頭文字で見分ける /
   タグは絞り込みの意味そのもの）。SPEC-V3 §1.4 の表に 1 行追記済み。
6. **UI-SPEC §5「並び替えシート（⇅）の先頭に『絞り込みをすべて解除』」（設計案 `2e`）**
   → **衝突。決定: 並び替えシートから外す**（絞り込みの解除は絞り込みシートに一本化）。
   同じことをする経路を 2 つ作らない、は SPEC-V3 §4.5 でも使った原則。**UI-SPEC 更新済み。**
7. **UI-SPEC §1.2 派生決定「期間シートの月グリッドは絞り込みを無視する」**
   → **矛盾しない。維持する**（§4.5）。タグでもグリッドの濃淡は変えない。
8. **SPEC-V3 §5.3「17 列」**
   → **18 列に改訂する**（§5.3）。CSV が未実装のうちに改訂するので、
   「後から列を足す」経路は発生しない。
9. **SPEC-V3 §0「追加するのは新テーブル 1 本と新しい列 1 本だけ」**
   → V3 の範囲の話なので矛盾しない。本書は**新テーブル 2 本を足し、`sale_records` には列を足さない**。
10. **SPEC-V2 §4.2「種別フィルタ」**
    → 条件そのものは変えない（`kind` の等値）。**UI の置き場所だけがチップからシートへ移る。**
    `kindFilter.ts` はそのまま使う。

---

## 9. 決定事項（旧「要確認」）

**2026-08-11 に 1〜14 を確定。未確定の項目は残っていない。**
番号は本書内から参照する用。**15 は実装時の調査項目**（決定ではなく作業。SPEC-V3 §8-14 と同じ扱い）。

1. **合計行の段の割り付け（§4.1 / §8-1 / §8-2）**
   **決定: (a) 1 段目 = 集計 2 値、2 段目 = セグメント ＋「絞り込み N」チップ。**
   集計は常に見えているべきで、セグメントは操作なので下に置く方が自然。
   **UI-SPEC §1.2 の「上部の固定段を 3 段に収める」は改訂する** ── 通常 4 段
   （ヘッダ / 月バー / 集計 2 値 / セグメント ＋ チップ）、絞り込み中はさらに解除バーで 5 段。
   （§4.1 に反映。UI-SPEC §1.2 を更新済み）
2. **「絞り込み N」の N の数え方（§4.1）**
   **決定: 条件の本数を数える**（種別 1 ＋ 販売サイト 1 ＋ タグ 1 = 最大 3）。
   タグを 3 つ選んでも OR なので条件としては 1 本。解除バーの文言（§4.3）と数が一致する。
   （§4.1 に反映）
3. **記録フォームの並び（§3.1 / §8-3）**
   **決定: 商品名 → タグ → 種別 → 金額の積み上げ。**
   種別は「金額の積み上げの直前」を保ち、タグは金額ではないのでその前に入る。
   （§3.1 に反映。UI-SPEC §6-6 を更新済み）
4. **計算タブにタグを置くか（§3.4）**
   **決定: 置かない。** 分類は記録するときに決めるもので、試算の段階では決まっていない。
   `siteName` のように state で引き継ぐこともしない。（§3.4 に反映）
5. **タグ名の上限（§1.3）**
   **決定: 1〜12 文字（書記素単位）で確定。** プリセットの 20 文字と違えるのは、
   タグが横に並ぶチップとして出るため。（§1.3 に反映）
6. **「・」をタグ名で禁じるか（§1.3 / §5.2）**
   **決定: 禁じる。** CSV の区切り文字を入力の側で予約する。
   エスケープ規則を設計するより説明も実装も短い。（§1.3 / §5.2 に反映）
7. **初期値（seed）（§1.2）**
   **決定: 置かない。0 件から始める。** タグの語は扱う商材で決まるので、
   汎用の初期値は消す手間しか生まない。記録フォームの検索欄から作れる（§3.2）ので詰まらない。
   （§1.2 に反映）
8. **色の自動割り当て（§1.2）**
   **決定: 採用。使用済みの色を避けて順に割り当て、編集で変更できる。**
   8 色すべてが使われていたら先頭から一巡する（重複は許す。名前が本体なので色が被っても読める）。
   一覧の「＋ 追加」からも同じ規則で入り、編集シートの色欄で変えられる。（§1.2 に反映）
9. **絞り込みの状態の寿命（§4.2 / §6）**
   **決定: (a) 画面ローカルの state のまま。** 永続化しない。記録タブとデータタブでも共有しない。
   保存すると「なぜ絞られているか分からない」状態で起動し得る。現状の `kindFilter` と同じ扱い。
   （§4.2 / §6 に反映）
10. **販売サイトの候補が多いときの形（§4.2）**
    **決定: 上限なし・検索欄なしで始める。** 実際に多くなってから対応する。
    候補は「記録に実在する `site_name`」なので、増えるのは実際に使ったサイトの数だけ
    （プリセットの登録数では増えない）。（§4.2 に反映）
11. **タグの登録件数の上限（§1.2）**
    **決定: 上限なし。** 選択シートに検索欄がある（§3.2）ので、増えても選べる。（§1.2 に反映）
12. **レコード詳細でのタグの位置（§3.4）**
    **決定: メタ行の下・商品名の直後。レシートカードの外側に置く。**
    レシートカードは「販売価格 − 内訳 = 結果」の金額の面（UI-SPEC §1.4）で、
    タグは金額ではないため中に入れない。（§3.4 に反映。UI-SPEC §1.4 を更新済み）
13. **絞り込みで 0 件になったときの空表示（§4.5 / §4.7）**
    **決定: 「条件に合う記録がありません」に統一し、その下に「絞り込みを解除」のリンクを置く。
    個別の条件名は出さない。** 条件が 5 つ（状態・期間・検索・種別・販売サイト・タグ）に増え、
    組み合わせごとに文言を作ると爆発する。**既存の `ListEmpty` の 2 通りの出し分け
    （検索 / 種別）も同じ 1 文言に統合する。**（§4.8 に反映）
14. **タグを CSV の書き出し対象の絞り込みに使うか（§5.4）**
    **決定: 使わない。** `ExportSheet` が持つのは期間と対象だけ。
    CSV はバックアップを兼ねる（SPEC-V3 §0.1）ので、絞った書き出しは
    「一部しか保存されていないファイル」を作る事故の方が大きい。（§5.4 に反映）

### 実装時の調査項目

15. **`db.transaction()` が両ドライバで使えるか（§1.4）**
    - expo-sqlite の drizzle sync ドライバと better-sqlite3 の双方で、
      同じ書き方でトランザクションを張れるかを**実装時に確認する**。
    - 使えない場合、中間テーブルの後始末（タグ削除 / 記録削除 / タグの付け替え）が
      途中で失敗する経路の扱いを決め直す。代替は「起動時に孤児行を掃除する」だが、
      §1.4 で退けた選択肢なので、採るなら理由を書き足すこと。
    - **Step 1 の中で確認する**（後の Step からは触らない層のため）。
